# TWOCOMMS — ГЛОБАЛЬНЫЙ АУДИТ-ЧЕКЛИСТ (twocomms_global_audit.md)

**Дата создания:** 05.07.2026 · **Версия:** 2.0 (итерация 2 — глубокий аудит кодовой базы + матрица рисков)
**Автор анализа:** AI-архитектор (глубокий reverse engineering репозитория + живой БД MySQL на сервере; итерация 2 — статический аудит всей кодовой базы, скриптов, статики, гигиены репозитория)
**Назначение:** исчерпывающий чек-лист (150+ атомарных пунктов) для агентов-исполнителей. Здесь НИЧЕГО не исправлено — только анализ и задачи.
**Связанные документы:** `TECHNICAL_TASKS.md` (реестр TECH-NNN), `../SITE_IMPROVEMENT_BACKLOG.md`, `../ANALYTICS_AND_TRACKING_BLUEPRINT.md`, `../MASTER_PLAN_V2.md`

---

## 0. ПРАВИЛА ДЛЯ АГЕНТОВ-ИСПОЛНИТЕЛЕЙ

1. **Один пункт = одна атомарная проверка/фикс.** Выполнил → ставь `[x]` и запись в «Журнал» внизу файла (дата, ID, коммит/PR).
2. **Формат каждого пункта:** «Где проверять» (путь/URL) + «Что искать/тестировать». Если баг подтверждён — сначала фиксируй находку в журнале, потом чини отдельным коммитом с ID пункта в сообщении.
3. **Безопасность:** SSH/MySQL-реквизиты — ТОЛЬКО у владельца. ЗАПРЕЩЕНО записывать пароли, ключи, DSN в любые файлы репозитория, коммиты, логи и отчёты. Перед каждым коммитом — проверка `git diff` на секреты.
4. **Боевая БД — MySQL на сервере** (не локальная SQLite). Все проверки данных — через SSH → `source ~/virtualenv/TWC/TwoComms_Site/twocomms/3.14/bin/activate && cd ~/TWC/TwoComms_Site/twocomms && python manage.py shell`. Только read-only запросы без явного согласования записи.
5. **Бренд-контекст:** эстетика TwoComms строится вокруг «трудностей/преодоления» (difficulties), НЕ спорт/бейсбол. Основной продукт — лонгсливы и худи (футболки вторичны). Тексты магазина транслируют концепцию бренда, а не личность владельца («я» — убирать).
6. Ничего не удалять/не рефакторить «заодно». Анти-задачи из `TECHNICAL_TASKS.md` действуют.

---

## ФАКТЫ, ПОДТВЕРЖДЁННЫЕ ЖИВОЙ БД (замер 05.07.2026)

Эти цифры — базовая линия. Агенты сверяются с ними до/после фиксов.

| Метрика | Значение | Вывод |
|---|---|---|
| Товары (Product) | 68 | каталог небольшой, аудит покрывает 100% карточек |
| Заказы (Order) | 41 (36 done, 5 cancelled) | статусная модель примитивна: нет shipped/delivered/RTS |
| UTM-сессии (UTMSession) | 1015 | трекинг сессий работает |
| UTM-сессии `is_converted=True` | **0** | **разрыв №2 подтверждён: конверсия источника не считается** |
| Заказы с utm_source | **0 из 41** | **разрыв №1 подтверждён: ни один заказ не привязан к UTM** |
| Заказы с utm_session FK | **0 из 41** | link_order_to_utm фактически не срабатывает в COD-потоке |
| CustomPrintLead | 28, **все в статусе `new`** | разрыв №3 подтверждён: воронка кастома мертва |
| UserAction всего | 36 859 | события пишутся |
| product_view | 36 009 (97,7% всех событий) | аномально много → подозрение на ботов/двойной счёт |
| add_to_cart | 44 | CTR view→cart = 0,12% — либо трекинг завышает views, либо UX-проблема |
| initiate_checkout / lead / purchase | 5 / 5 / 3 | purchase-события есть только у monobank-потока |
| SiteSession `is_bot=True` | 0 из 2899 | бот-детект не помечает никого → шум в данных |
| Top utm_source | google 542, ig 198, Instagram 128, **chatgpt.com 109** | AI-трафик уже 3-й источник; ig/Instagram — дубли (нет нормализации) |
| Кэш (django cache roundtrip) | OK (Redis работает) | Redis подключён и жив |
| Celery | удалён, работают no-op шимы | тех. долг: мёртвые упоминания в коде/логах |

## ФАКТЫ, ПОДТВЕРЖДЁННЫЕ СТАТИЧЕСКИМ АУДИТОМ КОДА (итерация 2, замер 05.07.2026)

Все цифры получены прямыми замерами по репозиторию (wc/grep/du/git ls-files). Это базовая линия для оптимизации.

| Метрика | Значение | Вывод |
|---|---|---|
| Размер репозитория (без .git) | **328 MB** | скриншоты/артефакты/бинарники в git раздувают клон |
| Файлов-артефактов в git (`artifacts/`, `output/`, `tmp/`) | **132** | PNG-скриншоты и lighthouse-JSON закоммичены — мусор |
| .md-отчётов в корне репо | **202** (+4 .txt) | корень нечитаем; история фиксов вместо docs/archive |
| Loose-скриптов в корне | **30 .py + 19 .sh** | 6+ конкурирующих deploy-скриптов, никто не знает какой рабочий |
| Backup-файлов, отслеживаемых git | 4 (`views.py.backup` 7790 строк, `styles.css.bak2` 445KB, `order_success_old.html`, `tmp_old_index.html`) | мёртвый код в git |
| Широких `except Exception`/`except: pass` | **697** | ошибки массово глотаются молча — отладка вслепую |
| `print()` в боевом Python-коде | **120** | вместо logging; засоряет stdout Passenger |
| TODO/FIXME/HACK в коде | 29 | зафиксированный, но не оттреканный долг |
| Крупнейший файл | `management/views.py` — **8188 строк** | god-file; +3270 строк `storefront/views/admin.py`, 2935 `storefront/models.py` |
| Middleware в цепочке | **26** | каждый запрос проходит 26 слоёв; порядок хрупкий |
| `@csrf_exempt` в коде | **39** | каждое место = потенциальная дыра CSRF |
| `mark_safe` / `\|safe` в шаблонах | 24 / 17 | точки XSS-риска при генерируемом контенте |
| Миграций суммарно | 264 (storefront 74, management 74, orders 46, accounts 28…) | долгие деплой-миграции; кандидаты на squash |
| CSS суммарно | 3.5 MB: `styles.css` **565KB**, `management.css` 234KB, `custom-print-configurator.css` 160KB, `finance.css` 141KB | не минифицировано, монолиты |
| JS суммарно | 1.0 MB: `custom-print-configurator.js` 148KB, `main.js` 83KB, `analytics-loader.js` 56KB | + **56 console.log** в бою |
| Дублирующиеся static-директории | `static/img/` 12MB и `static/images/` 6.3MB | два соглашения, дубли ассе����о�� |
| Inline `<script>` в base.html | 16 (файл 1403 строки) | блокирующий инлайн-JS, кэш не работает |
| requirements.txt | 81 строка, **3 без пина версии** (openai, google-auth, google-analytics-data) | недетерминированный деплой |
| Тестовых файлов | 177 (почти все — finance/management) | storefront/orders (деньги!) почти без тестов |
| SSH-доступ к серверу | сервер сбрасывает частые подключения (`kex_exchange_identification: reset`) | rate-limit: автоматизация должна батчить команды в 1 сессию |

---

## РАЗДЕЛ 1. UX/UI И ВОРОНКА КОНВЕРСИИ (CRO)

### 1.1 Главная страница

- [x] **CRO-001. Соответствие концепции «difficulties».** ✅ Аудит 05.07.2026: спорт-лексики нет; НО футболки первыми в title/subtitle/витрине (3 находки) → `audit_report_section1_homepage.md` Где: `twocomms_django_theme/templates/pages/index.html`, https://twocomms.shop/. Что: hero-заголовок, подзаголовки и CTA транслируют «преодоление/трудности», нет спортивной/бейсбольной лексики; лонгсливы и худи визуально приоритетнее футболок.
- [x] **CRO-002. Работа всех CTA-кнопок главной.** ✅ Аудит 05.07.2026: desktop OK, все URL 200/0-redirect; **P0-БАГ: на mobile ≤360px hero-CTA полностью обрезаны** (base.html: hero height:60vh+overflow:hidden) → `audit_report_section1_homepage.md` Где: index.html + https://twocomms.shop/. Что: каждая кнопка (в каталог, в карточку, в кастом) кликабельна, ведёт на корректный URL, не даёт 404/редирект-цепочек; проверить на мобильном viewport 360px.
- [x] **CRO-003. Скорость рендера главной (LCP/CLS/INP).** ✅ Аудит 05.07.2026 (lab): LCP 1.78s / CLS 0.00 / TTFB mobile 518ms — зелёная зона; PSI field — владельцу → `audit_report_section1_homepage.md` Где: PageSpeed Insights / Lighthouse mobile для https://twocomms.shop/. Что: LCP < 2.5s, CLS < 0.1, INP < 200ms; зафиксировать текущие числа в журнале как базовую линию.
- [x] **CRO-004. Отложенная загрузка GTM не ломает атрибуцию.** ✅ Аудит 05.07.2026: ЛОМАЕТ — fast-path для utm/fbclid отсутствует, bounce с рекламы невидим для пикселей; GTM home ждёт 25–35s; фикс P1 ~10 строк → `audit_report_section1_homepage.md` Где: `base.html` (строки ~929–996: GTM грузится по interaction или таймауту 12–20s). Что: пользователь, зашедший с рекламы и ушедший до interaction, теряется для Pixel/GA4 — замерить долю таких сессий; решить: сократить таймаут для сессий с utm_* в URL.
- [x] **CRO-005. Блок отзывов на главной.** ✅ Аудит 05.07.2026: блока нет; reviews-инфраструктура готова (работает на PDP) — TECH-013 = переиспользование, P2 → `audit_report_section1_homepage.md` Где: index.html, `reviews/`. Что: есть ли вывод реальных отзывов; если блока нет — задача TECH-013.
- [x] **CRO-006. Мёртвые/неиспользуемые секции главной.** ✅ Аудит 05.07.2026: мёртвого DOM нет; survey-флаг корректен; рекомендация — survey ниже товаров → `audit_report_section1_homepage.md` Где: index.html. Что: секции, скрытые через `display:none`/комментарии, которые всё равно рендерятся и грузят DOM.

### 1.2 Каталог

- [x] **CRO-010. Фильтрация лонгсливов/худи.** ✅ Аудит 05.07.2026: категория/цвет работают (URL-шаримы, чипы сохраняют GET); фильтра по размеру НЕ существует; всё full-page reload → `audit_report_section1_catalog.md` Где: `storefront/views/catalog.py` (1215 строк), `pages/catalog.html`, URL `/catalog/long-sleeve/`, `/catalog/hoodie/`. Что: фильтры по категории (slug: long-sleeve id=5, hoodie id=4, tshirts id=3), цвету, размеру работают без полной перезагрузки; выбранный фильтр сохраняется в URL (шаримая ссылка).
- [x] **CRO-011. Пагинация каталога.** ✅ Аудит 05.07.2026: **P1-БАГ №1: `?page=N` сбрасывает `?color=` фильтр**; **P1-БАГ №2: `/catalog/?page=2..N` — индексируемые дубли без товаров** (корень не рендерит грид); категорийная пагинация OK (0 дублей, prev/next/canonical корректны) → `audit_report_section1_catalog.md` Где: `storefront/pagination.py`, catalog.py. Что: canonical/prev/next на страницах пагинации; переход по страницам не сбрасывает фильтры; нет дублирующихся товаров между страницами.
- [x] **CRO-012. Lazy-load изображений каталога.** ✅ Аудит 05.07.2026: lazy OK, width/height на 100% карточек (CLS=0); НО eager только 2 (не 4–8) и у 9/16 карточек нет responsive AVIF/WebP (media/product_colors/ без optimized-вариантов) → `audit_report_section1_catalog.md` Где: catalog.html, `optimized_image.html`, `responsive_image.html`, `image_optimizer.py`. Что: `loading="lazy"` на всех карточках ниже фолда, первые 4–8 карточек eager; корректные width/height (нет CLS).
- [x] **CRO-013. Порядок сортировки: лонгсливы/худи первыми.** ✅ Аудит 05.07.2026: НАРУШЕНО — top-5 priority все футболки (68…64), max priority tshirts 68>hoodie 63>long-sleeve 51; showcase-хардкод 02=футболки выше худи; Category.order все =0 (алфавит) → `audit_report_section1_catalog.md` Где: catalog.py (queryset ordering), главная витрина. Что: дефолтная сортировка не выпячивает футболки вопреки позиционированию.
- [x] **CRO-014. Статусы наличия в каталоге.** ✅ Аудит 05.07.2026: механики наличия НЕТ — все 75 вариантов stock=0 (поле мертво), размерного склада нет; archived скрыты (phantom-SKU в гриде нет), но реальное «закончилось» системно не ловится → `audit_report_section1_catalog.md` Где: catalog.html, `storefront/models.py` (Product). Что: карточки «нет в наличии» либо честно помечены, либо скрыты (TECH-010); нет phantom-SKU, на которые можно кликнуть и застрять.
- [x] **CRO-015. N+1 при рендере каталога.** ✅ Аудит 05.07.2026 (замер на бою, CaptureQueriesContext): N+1 нет; корень 14, категории 19, цвет-фильтр 12 SQL; page-cache 600s → 0 SQL по����торно; бонус: DEBUG=True роняет прод (OfflineGenerationError) → `audit_report_section1_catalog.md` Где: catalog.py + `storefront/services/catalog/`. Что: `select_related`/`prefetch_related` для категори��, цвето��ых вариантов (`productcolors`), изображений; замерить кол-во SQL-запросов на страницу каталога (django-debug-toolbar локально или логирование запросов), норма ≤ 15.

### 1.3 Карточка товара

- [x] **CRO-020. Корректность данных из MySQL.** ✅ Аудит 05.07.2026: 10/10 случайных товаров — полное совпадение БД↔сайт (H1/цены/скидки/цвета); побочно: published=65 (не 68), RU/EN-описания — заглушки ~120 симв., двойной product_view на legacy-URL (record до 301), на PDP есть 5 JSON-LD блоков (SEO-020 опровергнут) → `audit_report_section1_product.md` Где: `storefront/views/product.py` (755 строк), `pages/product_detail.html`; живая БД: 68 товаров. Что: для 10 случайных товаров сверить название, цену, описания, цвета (`productcolors.ProductColorVariant`), фото на сайте против значений в БД (через SSH shell, read-only).
- [x] **CRO-021. Тексты без «я», в tone of voice бренда.** ✅ Аудит 05.07.2026: все 65 PDP просканированы (SSH заблокирован rate-limit'ом — скан живых страниц) — первого лица владельца 0, все 27 regex-совпадений это названия принтов («Це Моя Посадка» и т.п.); спорт-лексики 0; побочно P2: 65/65 описаний с идентичным boilerplate-манифестом (near-duplicate) → `audit_report_section1_texts.md` Где: описания в БД (`Product.description*`, модели переводов django-modeltranslation: name_uk/ru/en), product_detail.html. Что: grep по описаниям на «я », «мне», «мой» от первого лица владельца; спорт/бейсбол-лексика; несоответствие концепции «difficulties».
- [x] **CRO-022. Фото товара: варианты и вебп.** ✅ Аудит 05.07.2026: фото 65/65, alt уникальны и непусты 65/65; НО P1 — у 20/65 товаров LCP-фото без optimized-вариантов (404 на диске, оригиналы 100–276 KB vs 12 KB у здоровых; причина — старый bind-баг inline-fallback в optimize_image_field_task, нужен backfill `manage.py optimize_images`) → `audit_report_section1_images.md` Где: `image_optimizer.py`, `storefront/services/image_variants.py`, media на сервере. Что: все карточки имеют основное фото; генерируются ли webp/размеры; alt-тексты не пустые и не дублируются.
- [x] **CRO-023. Размерная сетка с карточки.** ✅ Аудит 05.07.2026: P1 — у всех лонгсливов нет таблицы замеров (нет `longsleeve` preset/catalog/SizeGrid, `/rozmirna-sitka/` тоже без лонгсливов); `view_size_guide` отсутствует; seo-pricing блок даёт 24 битые ссылки `/product/{id}/` → 404 на 3 категориях → `audit_report_section1_product.md`. Что: SizeGrid/пресет longsleeve, привязки catalog, fix `get_absolute_url`, TECH-005.
- [x] **CRO-024. Событие product_view — завышение.** ✅ Аудит 05.07.2026: 3 root cause в коде — `record_user_action` без bot-фильтра, двойной счёт на 301 legacy URL (`record_product_view` до redirect), нет дедупа повторных просмотров; сервер+JS дубля и prefetch-триггера нет → `audit_report_section1_product.md`. Что: bot-filter, перенос записи после redirect, дедуп 30 мин.
- [x] **CRO-025. Выбор цвета/размера — UX и трекинг.** ✅ Аудит 05.07.2026: смена фото/URL/canonical работает, но цена не обновляется по `price_override`, stock не блокирует выбор/корзину, `select_size`/`select_color` отсутствуют, a11y свотчей неполная → `audit_report_section1_product.md`. Что: синхронизировать price_override/stock и добавить TECH-008 события.
- [x] **CRO-026. Блок отзывов на карточке.** ✅ Аудит 05.07.2026: инфраструктура образцовая (модель/модерация/schema из approved, порог=1), НО live-скан 48/48 PDP — 0 AggregateRating = 0 опубликованных отзывов; петля сбора (заказ доставлен → запрос отзыва) НЕ реализована — P1; vote-endpoint без UI (P2); тексты рассинхронены с порогом 1 (P3) → `audit_report_section1_product.md` Где: `reviews/`, product_detail.html. Что: выводятся ли отзывы; schema Review/AggregateRating (см. SEO-раздел).
- [x] **CRO-027. Рекомендации на карточке.** ✅ Аудит 05.07.2026: N+1 НЕТ (bulk prefetch + fragment-cache 3600s); НО out-of-stock не фильтруется (системно, → stock-enforcement CRO-025), дубли товаров в выдаче без дедупа (P2), бесполезная per-user фрагментация кэша + TTL-рассинхрон 300/3600 (P3), отменённые заказы искажают «часто покупают вместе» (P3) → `audit_report_section1_product.md` Где: `storefront/recommendations.py`. Что: блок «с этим покупают/похожие» не делает N+1 и не рекомендует out-of-stock.

### 1.4 Мини-корзина и корзина

- [x] **CRO-030. Логика мини-корзины vs full-page корзины.** ✅ Аудит 06.07.2026: архитектура задокументирована (session-first + UserCart JSON-снапшот, merge на `user_logged_in` без дублей, ревизия sha256); **P2-БАГ: `user_state_hint` (context_processors.py:57) читает `custom_cart` вместо `custom_print_cart` → бейдж 0 при кастом-only корзине**; P2: `/cart/count/` игнорирует кастом; P2: GET `cart_summary` мутирует сессию и сбрасывает monobank-инвойс; P2: multi-device lost-update (LWW-снапшот) → `audit_report_section1_cart.md` Где: `storefront/views/cart.py` (1850 строк — кандидат на декомпозицию), `accounts/cart_middleware.py`, `cart_models.py`, `cart_signals.py`, `cart_sync.py`, `pages/cart.html`. Что: описать в отчёте фактическую архитектуру (session-корзина + синхронизация в БД для залогиненных); проверить сценарии: добавил анонимно → за��огинился → корзина слилась без дублей.
- [x] **CRO-031. Баги добавления/удаления.** ✅ Аудит 06.07.2026: 2 позиции при двойном клике не создаются (мерж по ключу), но **P2: нет in-flight guard в `main.js` → двойной клик = qty 2 + двойные пиксели**; удаление последней позиции обнуляет б��йдж (кроме кастом-кейса CRO-030); смена qty пересчитывает сумму+промокод корректно; **P1: `update_cart`/`remove_from_cart` не сбрасывают Monobank-инвойс → оплата устаревшей суммы при сбое JS**; P2: fallback remove удаляет все варианты товара → `audit_report_section1_cart.md` Где: cart.py (AJAX-эндпоинты), JS в base.html/cart.html. Ч��о: быстрый двойной клик «добавить» не создаёт 2 позиции; удаление последней позиции корректно обнуляет счётчик в шапке; смена количества пересчитывает сумму и промокод.
- [x] **CRO-032. Кэширование не ломает мини-корзину.** ✅ Аудит 06.07.2026: архитектура cache-safe (кэш только анонимам, бейдж SSR=0 + AJAX no-store, `/cart/` never_cache — live-подтверждено); **P1-БАГ (подтверждён live 2-мя curl-сессиями): кэшированные страницы отдают чужой csrfmiddlewaretoken → language switcher POST /i18n/setlang = 403 для всех анонов на cache-hit (TTL 600s)**; P2: `/cart/summary/` и `/cart/count/` без `Cache-Control: no-store` (сервер LiteSpeed); P3: `user_state_hint` — мёртвый код (не зарегистрирован в context_processors, коррекция CRO-030 №1); P3: главная/каталог без Cache-Control вообще; P3: опасные неиспользуемые декораторы `cache_views.py` → `audit_report_section1_cart.md` Где: `twocomms/cache_headers.py`, `twocomms/media_cache_middleware.py`, `cache_utils.py`, `storefront/cache_signals.py`. Что: страницы с корзиной/счётчиком не отдаются из полного page-cache другому пользователю; заголовки Cache-Control на HTML с динамикой = private/no-cache; счётчик корзины подтягивается AJAX-ом после загрузки кэшированной страницы.
- [x] **CRO-033. Событие add_to_cart — сервер + пиксели.** ✅ Аудит 06.07.2026 (код; live Test Events требуют доступа к кабинетам — оставлено ручным шагом): цепочка задокументирована — 1 клик = 1 POST = 1 UserAction + 1 fbq + 1 ttq с общим event_id (дедуп-задел корректный, ui-fallback — мёртвая ветка); **P2: dataLayer получает ДВА события (`AddToCart` из trackEvent + `add_to_cart` из pushAddToCartEvent) → риск двойного счёта GA4, проверить триггеры GTM**; **P2: server-side CAPI отсутствует полностью (grep=0) — при блокировщиках пиксели слепнут, кандидат в объяснение 36009→44 ATC**; P2: двойной клик = 2 события с разными event_id (закрывается guard-ом из CRO-031); P3: кастом-принт шлёт пиксельный AddToCart без серверного record_add_to_cart → `audit_report_section1_cart.md` Где: cart.py (вызовы `record_add_to_cart`), `static` JS (fbq/ttq AddToCart). Что: одно добавление = ровно одно серверное UserAction + одно событие в каждый пиксель; протестировать вживую через Meta Test Events / TikTok Test Events.
- [x] **CRO-034. Кастом-принт позиции в корзине.** ✅ Аудит 06.07.2026: ядро сценария подтверждено кодом — pending-кастом НЕ блокирует оформление обычных товаров (COD + Monobank), UI объясняет ожидание образцово (бейджи, payment_note, «Орієнтовно за кастом», модерационный блок); rejected авточистится, draft автопромоутится, Monobank не теряет кастом при брошенном инвойсе; **P2: COD-чекаут не проверяет цену approved-кастома (лид с ценой 0 уедет бесплатно; Monobank блокирует 400-кой) — дубль-логика split разъехалась**; **P2: COD молча удаляет из сессии кастом-записи без `lead_id` — потеря данных**; P3: мёртвый флаг `payment_allowed` (3 формулы), рассинхрон при удалённом из БД лиде (UI покажет большую сумму, чем инвойс), промокод дисконтирует согласованную цену кастома, `custom_print_remove` не сбрасывает monobank-инвойс (родня №5 CRO-031), брошенный инвойс не отвязывает `lead.order`, malformed-атрибут в cart.html:925. Гл. рекомендация: вынести `_split_custom_cart_entries` в общий модуль для обоих потоков → `audit_report_section1_cart.md` Где: `storefront/custom_print_config.py` (SESSION_CUSTOM_CART_KEY), checkout.py (split approved/pending). Что: pending-кастом не блокирует оформление обычных товаров; UI корзины ясно объясняет, почему кастом «ждёт».
- [x] **CRO-035. Восстановление корзины.** ✅ Аудит 06.07.2026: корзина переживает закрытие браузера для ОБОИХ типов юзеров — гости: session-cookie 30 дней (cached_db); залогиненные: `UserCart` (JSONField cart+custom+промо) + `CartSyncMiddleware` (регистрирован в dev и prod, корректно после Auth) �� content-hash-ревизией, persist только при реальном изменении и статусе 2xx/3xx, login-мерж гостевой корзины через `user_logged_in`-сигнал, есть tests (`test_cart_sync.py`); все 4 фикса из `CART_RESTORATION_REPORT.md` проверены построчно — реально в коде (ключ `id:size:cv`, ответ `ok/count/total`, параметр `key` согласован JS↔сервер, временный views.py удалён); проблемы — 5×P3: кросс-девайс-гидрация не сбрасывает monobank-инвойс (новый вектор бага №5 CRO-031), last-write-wins теряет параллельную мутацию со 2-го устройства (узкое окно), get_or_create UserCart на каждом авторизованном запросе (перф), устаревший docstring формата ключа, отчёт в корне репо предшествует DB-sync-слою → `audit_report_section1_cart.md` Где: cart_middleware.py, отчёты `CART_RESTORATION_REPORT.md` (корень репо). Что: корзина переживает закрытие браузера для залогиненных; отчёт в корне числит фиксы «сделанными» — проверить фактически.

### 1.5 Чекаут

- [x] **CRO-040. Полный прогон COD-чекаута.** ✅ Аудит 06.07.2026: **P0 — полноценный guest COD отсутствует/сломан**: в UI корзины нет `cod` и нет submit-кнопки COD, только Monobank Pay; ручной live-submit `form_type=guest_order` с валидными NP signed tokens даёт HTTP 500 (`process_guest_order` отсутствует после refactor), заказ не создаётся; NP lookup живой, phone validation живая только на прямом `/orders/create/`; AddToCart даёт CSP console errors Google Ads `www.google.com.ua/pagead` → `audit_report_checkout_critical.md` Где: `storefront/views/checkout.py::create_order`, https://twocomms.shop/ (тестовый заказ с пометк��й). Что: минимальный набор полей, валидация телефона (`normalize_checkout_phone`), Нова Пошта подбор отделения (`orders/nova_poshta_*`); 0 JS-ошибок в консоли на всём пути.
- [x] **CRO-041. ��РИТИЧНО: COD-��������аказ не пишет UTM.** ✅ Аудит 05.07.2026: P0 подтверждён кодом+БД; `checkout.py::create_order` не вызывает `link_order_to_utm`/`record_order_action`, COD-заказы не пишут `session_key`, а `link_order_to_utm` не срабатывает и в monobank-потоке без fallback на session/visitor_id → `audit_report_section1_cro.md`. Что: TECH-060 — единая UTM-привязка любого заказа + fallback-цепочка.
- [x] **CRO-042. is_converted никогда не проставляется.** ✅ Аудит 05.07.2026: P0 подтверждён — 0/1015 UTMSession converted; `record_purchase` фактически мёртв, а `record_lead`/`record_order_action` не находят UTMSession из-за разрыва session_key/utm_session → `audit_report_section1_cro.md`. Что: TECH-061 — конверсия через order/visitor/session fallback после фикса CRO-041.
- [x] **CRO-043. Monobank-поток: вебхук и статусы.** ✅ Аудит 06.07.2026: проверено и отмечено в отчёте → `audit_report_payment_security.md`; идемпотентность `purchase` при повторном success в основном выдержана (DB: repeated success в history, но purchase-дублей по order_id нет); **P0: подпись webhook НЕ проверяется**, основной webhook доверяет body; **P0: `monobank_return` использует unsafe fallback `status_value or 'success'`**; failure не ведёт на `order_failed.html`, а возвращает в `/cart/` с message, cart не очищается до success. Где: `storefront/monobank.py`, `storefront/views/monobank.py` (986 строк вызова record_lead). Что: вебхук идемпотентен (повторный callback не создаёт второй purchase-event); подпись вебхука проверяется; отказ оплаты ведёт на `order_failed.html` с восстановимой корзиной.
- [x] **CRO-044. Страница «Спасибо за покупку».** ✅ Аудит 06.07.2026: проверено и отмечено в отчёте → `audit_report_checkout_critical.md`; **P0: `/orders/success/<id>/` публично отдаёт PII и `purchase-payload` анониму**, preview тоже 200; server-side `UserAction purchase/lead` дублей по order_id не найдено, event_id deterministic, но client-side дедуп только `sessionStorage`+server flag и может повторно стрелять в новом browser context; upsell слабый (CTA каталог/аккаунт/receipt/Instagram REVIEW10, нет персональных рекомендаций); `order_success_old.html` подтверждён как tracked unused backup, удаление остаётся TD-002/CB-004 исполнителю. Где: `pages/order_success.html` (+ мёртвый `order_success_old.html`). Что: серверное purchase-событие не дублируется при F5 (event_id-дедуп); есть ли рекомендации/апселл; `order_success_old.html` — удалить (тех. долг, см. TD-раздел).
- [x] **CRO-045. Определение purchase-момента.** ✅ Аудит 06.07.2026: матрица 4 слоя × 3 потока = 4 разных определения purchase; **P0: COD-покупки видит только Meta CAPI (через NP-крон), GA4/TikTok/UserAction — никогда**; уточнение: `record_purchase` мёртв, но живой `record_order_action('purchase')` в monobank.py:1241 пишет UserAction при paid/prepaid (объясняет 3 записи в БД), is_converted всё равно 0 из-за отсутствия utm_session-fallback; внутренний `lead` = создание инвойса ДО оплаты; prepaid шлёт полную сумму во все слои без refund-событий; TikTok paid-ветка без pre-check дедупа → `audit_report_checkout_critical.md` Где: monobank.py (purchase при оплате) vs COD (оплата при получении). Что: зафиксировать документально: purchase = создание заказа + отдельное серверное событие оплаты/доставки (TECH-066); привести GA4/Pixel/CAPI к одному определению.
- [x] **CRO-046. Промокоды в чекауте.** ✅ Аудит 06.07.2026: сообщения об ошибках понятны и специфичны (invalid/expired/exhausted/min-amount/auth) — требование выполнено; НО **P0: в COD-чекауте промокод не применяется никогда** (checkout.py:224 читает мёртвый ключ `promo_code` вместо `promo_code_id`, несуществующие `active=True`/`is_valid()` — клиент видит скидку в корзине, а заказ создаётся на полную сумму); **P0: one_time_per_user/групповые лимиты не работают** — `record_usage()` (пишущий PromoCodeUsage) имеет 0 call-sites, monobank зовёт голый `use()`; P1: prepay_200+промо озвучивает завышенный остаток (total_sum+discount двойной счёт), `use()` сжигает лимит при создании инвойса до оплаты; P2: `promo_code_data` не чистится, `coupon_apply` отсутствует (TECH-023 подтверждён) → `audit_report_checkout_critical.md` Где: `storefront/models.py` (PromoCode), cart.py/checkout.py. Что: невалидный/просроченный код даёт понятную ошибку; события `coupon_apply` (TECH-023) нет — задача на добавление.
- [x] **CRO-047. Ошибочные состояния чекаута.** ✅ Аудит 06.07.2026: пустая корзина → message+redirect ОК; `transaction.atomic` в обоих путях ОК; 500 не отдаётся (generic except+log) ОК; НО **P1: исчезнувший товар молча выбрасывается из заказа** (`if not product: continue` в обоих путях, без сообщения), **P1: COD-путь без guard total_sum<=0 → возможен заказ на 0 грн** (Monobank guard имеет), P1: складского контроля не существует как класса (у Product нет stock-поля — зафиксировать как продуктовое решение); P2: `monobank_create_invoice` и `clear_cart` внутри atomic → внешний API в транзакции + потеря корзины без заказа при сбое инвойса (cached_db сессии не откатываются); P3: стаб monobank_webhook в checkout.py → `audit_report_checkout_critical.md` Где: checkout.py. Что: пустая корзина → редирект с message; товар кончился между корзиной и заказом → понятное сообщение, а не 500; проверить транзакционность (`transaction.atomic`) создания Order+OrderItem.

### 1.6 Воронка целиком (замер)

 - [x] **CRO-050. Сквозной тест-прогон воронки с UTM.** ✅ Аудит 06.07.2026 (iter4, HTTP-слой): пр��гон curl `?utm_source=audit&utm_medium=test&utm_campaign=funnel_check` → каталог → PDP `/product/20-twocomms-legend/` → POST `/cart/add/` (200, `ok:true`, cart_count=1) → корзина с товаром; сабмит заказа НЕ выполнялся намеренно. Сессия для сверки в БД: sessionid=jrg8b3…, twc_vid=f2cd68…. Остаток — SSH-сверка UTMSession(audit)/UserAction (��атч в `AGENT_WORK_LOG_iter4.md` §6). Находки: offer_id «TC-0020-ЧЕРНЫЙ-M» — рус. цвет в SKU (NEW-406); legacy-дефолт pay_type='cod' в checkout.py:119 при отсутствии cod в UI (NEW-407). Где: браузер: `https://twocomms.shop/?utm_source=audit&utm_medium=test&utm_campaign=funnel_check` → каталог → карточка → мини-корзина → корзина → чекаут → заказ (тестовый). Что: после прогона в БД (SSH shell): UTMSession создана, UserAction содержит page_view→product_view→add_to_cart→initiate_checkout→purchase/lead, Order имеет utm_source='audit'. Сейчас последний шаг ГАРАНТИРОВАННО провалится (см. CRO-041) — это acceptance-тест фикса.
- [x] **CRO-051. Конверсия по шагам — базовая линия.** ✅ Аудит 07.07.2026 (SSH/живая БД): raw baseline обновлён до **40 490 product_view → 55 add_to_cart → 6 initiate_checkout/lead → 3 purchase**; unique-session funnel: 836 → 16 → 2 → 1 purchase; **96,23% product_view без site_session**, `is_bot=True` = 0/3443, поэтому raw-конверсия не UX-истина, а доказательство грязного трекинга; топ просмотров занят футболками (конфликт CRO-013) → `audit_report_section1_cro.md`

---

## РАЗДЕЛ 2. СКВОЗНАЯ АНАЛИТИКА И ПИКСЕЛИ

### 2.1 GTM / GA4

- [x] **AN-001. Инвентаризация GTM-контейнера GTM-PRLLBF9H.** ✅ Аудит 07.07.2026 (iter6, код-слой): полная карта входа контейнера — ДВА параллельных конвейера dataLayer (GA4-native: view_item/select_item/view_item_list/add_to_cart/begin_checkout/purchase/login и Meta-style через trackEvent: ViewContent/AddToCart/InitiateCheckout/Purchase/…) → каждый воронковый шаг = 2 события (риск двойного счёта при триггерах на оба имени); **P2: gtag.js G-109EFTWM05 грузится параллельно с GTM** (риск дубля GA4 page_view при GA4-теге внутри контейнера); P3-мёртвые артефакты: partials/analytics.html (163 строки, весь в comment, но include-ится), YM-ветка (data-ym-id нигде не выставлен), аноним. div#am с нечитаемыми атрибутами. Остаток — экспорт/сверка контейнера в GTM UI владельцем → `audit_report_section2_analytics.md` Где: base.html:973–992, GTM-интерфейс (доступ у владельца). Что: список всех тегов/триггеров/переменных; мёртвые теги; дубли GA4-событий (сервер+клиент).
 - [x] **AN-002. Отложенный GTM vs paid-трафик.** ✅ Аудит 06.07.2026 (iter4, код): подтверждено — fast-path для utm_*/fbclid/gclid/ttclid ОТСУТСТВУЕТ (grep по base.html и analytics-loader.js = 0 совпадений); вся UTM-атрибуция только server-side (`utm_middleware.py`), клиентские пиксели теряют paid-bounce. Фикс = CRO-004 (P1, ~10 строк: немедленная загрузка GTM при наличии click-id/utm в URL). Где: base.html (interaction-триггер + таймаут 12–20s, `isPassiveAnalytics`).
- [x] **AN-003. GA4-события воронки.** ✅ Аудит 07.07.2026 (iter6, код-слой): ядро есть и корректно (view_item_list/select_item/view_item/add_to_cart/begin_checkout с items[]+value/currency+event_id); **P1: purchase стреляет только у monobank paid/prepaid — COD-заказы для GA4 невидимы** (стык CRO-045, закрывается TECH-066/070); **P2: payment_type НЕ существует нигде (TECH-007 не выполнен)**; P2: add_shipping_info/add_payment_info отсутствуют (есть только Meta-style AddPaymentInfo/FindLocation вне ecommerce-схемы); P3: item_id листинга `TC-pid-default-S` не сматчится с реальным offer_id покупок. Остаток — GA4 DebugView-прогон владельцем → `audit_report_section2_analytics.md` Где: GTM + GA4 DebugView. Что: view_item, add_to_cart, begin_checkout, add_shipping_info, add_payment_info, purchase с items[] и value/currency; параметр payment_type (cod/prepay) — TECH-007.
- [x] **AN-004. Internal/staff-трафик исключён.** ✅ Аудит 06.07.2026 (код-слой): серверные писатели покрыты все 3, но `is_staff` НЕ исключается автоматически (нужны ручные правила в AnalyticsExclusion), клиентские пиксели не покрыты вовсе; остаток — count() правил в проде по SSH → `audit_report_section2_analytics.md` Где: `storefront/analytics_exclusions.py` (is_request_excluded), GA4-фильтры.

### 2.2 Meta Pixel + CAPI

- [x] **AN-010. Pixel ID 823958313630148 — события клиента.** ✅ Аудит 07.07.2026 (iter6, код-слой): полный список fbq-call-sites задокументирован (16 типов событий), загрузка/буферизация/валидация/noscript-fallback образцовые, eventID-дедуп корректен; **P2: 4-й аргумент fbq несёт external_id/fbp/fbc/user_data — браузерный пиксель поддерживает только eventID, остальное игнорируется молча** (per-event user_data через браузер НЕ уходит); **P2: advanced matching (div#am) заполняется только у залогиненных — гостевой EMQ держится исключительно на CAPI+fbp/fbc**; P3: raw PII залогиненного в DOM (осознанный Meta-паттерн). Остаток — Meta Pixel Helper live-сверка владельцем → `audit_report_section2_analytics.md` Где: base.html:12, `analytics-loader.js` (static). Что: PageView, ViewContent, AddToCart, InitiateCheckout, Purchase реально стреляют (Meta Pixel Helper); advanced matching div#am заполняется.
- [x] **AN-011. CAPI-события сервера.** ✅ Аудит 07.07.2026 (код-слой): retry дублей НЕ создаёт (детерминированный event_id `{order_number}_{created_ts}_{type}` не перегенерируется между попытками + Meta-дедуп 48ч + флаг `payment_payload.facebook_events.purchase_sent` до отправки); карта call-sites: send_purchase_event — 2 живых пути (monobank utils.py:641, НП delivered nova_poshta_service.py:507), send_add_payment_info_event — monobank.py:1002, **send_lead_event и send_event_for_order_status — мёртвые API (0 call-sites)**; **P1: весь блок отправки Meta+TikTok живёт ВНУТРИ `transaction.atomic()`+`select_for_update()` — до ~25–40s удержания row-lock при retry/timeout**; P3: fallback `event_source_url` на чужой домен twocomms.com (4 места, боевой — twocomms.shop); P3: `time.sleep` блокирует воркер. Остаток SSH: боевые логи monobank_logger + валидность токена → `audit_report_section2_analytics.md`
- [x] **AN-012. Дедупликация Pixel↔CAPI.** ✅ Аудит 07.07.2026 (код-слой): TECH-064 в коде РЕАЛЬНО СДЕЛАН для Purchase (идентичный `order.get_purchase_event_id()` в fbq eventID order_success.html:2134 и CAPI utils.py:641) и AddPaymentInfo (`add_payment_event_id` из ответа ин��ойс��, checkout-mono.js:310/502); Lead-пары НЕ существует (серверный Meta Lead не шлётся — см. AN-011); COD-Purchase коллизии нет by design (клиен�� гейтит��я по paid/prepaid/partial); P3: клиентский fallback random-id у AddPaymentInfo рвёт пару в edge-case; `META_PIXEL_CAPI_DEDUPE_IMPLEMENTATION.md` соответствует коду. Остаток владельцем: блок Deduplication + EMQ в Events Manager (кодом не проверяется) → `audit_report_section2_analytics.md`
- [x] **AN-013. fbc/fbp/fbclid доходят до CAPI.** ✅ Аудит 05.07.2026: P0 подтверждён — Order не хранит click-ID, COD не пишет `payment_payload.tracking`, а CAPI берёт `fbc/fbp` только из tracking payload; fbclid без UTM тоже может полностью теряться → `audit_report_section2_analytics.md`. Что: расширение TECH-060 — копировать/synthesize click-ID для любого заказа.
- [x] **AN-014. Offline-конверсии delivered.** ✅ Аудит 06.07.2026: КОРРЕКЦИЯ — механизм СУЩЕСТВУЕТ: НП-трекинг (`update_tracking_statuses` → delivered ⇒ status='done'+payment_status='paid' → `_send_facebook_purchase_event`, идемпотентно через `facebook_events.purchase_sent`); статусов в модели 5 (new/prep/ship/done/cancelled), а не 2; НО `send_event_for_order_status` — мёртвый API (0 call-sites), **P1: delivered-Purchase уходит ТОЛЬКО в Meta (TikTok/GA4 — никогда)**, P2: нет refund/cancel-событий → ROAS завышен на невыкупы; крон на сервере — остаток SSH → `audit_report_section2_analytics.md` Где: facebook_conversions_service.py::send_event_for_order_status, orders/status_management.py. Что: возможна ли отправка события по факту доставки — блокируется TECH-070/071.
- [x] **AN-015. test_event_code изоляция.** ✅ Аудит 06.07.2026: FB CAPI test-код нигде не определён (всегда None — чисто); TikTok test-код клиент+сервер из env (риск «забытой» переменной — остаток SSH); исторически публичный `/test-analytics/` через 3s стрелял Purchase 599 грн в боевые пиксели. **Post-fix 16.07.2026 (F-010, `efd7f192`):** test/debug/dev URL регистрируются только при `DEBUG=True`; production подтвердил `DEBUG=False`, server tests 7/7 и live 21/21 hard 404, поэтому тестовая страница больше не может загрязнять production analytics → `audit_report_section2_analytics.md`.

### 2.3 TikTok Pixel

- [x] **AN-020. TikTok Pixel D43L7DBC77UA61AHLTVG.** ✅ Аудит 07.07.2026 (код-слой): исторический отчёт зафиксировал event-name/API/retry/COD остатки. **Post-fix 16.07.2026 (F-011, `c0b324c3`):** отсутствие inline `ttq.load` признано ожидаемой deferred-архитектурой; реальный paid-low bootstrap gap закрыт immediate idempotent init. Server 3/3, live asset hash совпал, intercepted matrix paid-low/organic-low/paid-normal прошла без дублей. Events Manager и остальные AN-020/AN-021 остатки этим фиксом не закрываются → `audit_report_section2_analytics.md`.
- [x] **AN-021. ttclid сквозной путь.** ✅ Аудит 06.07.2026: разрыв подтверждён — ttclid доезжает до TikTok CAPI только у monobank (`payment_payload.tracking` пишется одним monobank.py:972); COD-заказы без клик-ID; `UTMSession.ttclid` лежит в БД, но сервисами не читается — fallback описан → `audit_report_section2_analytics.md`

### 2.4 UTM-м��ханика

- [x] **AN-030. UTM пе��еживает всю воронку.** ✅ Аудит 06.07.2026: два хранилища — session['utm_data'] переживает логин, но `link_order_to_utm` его НЕ читает (только UTMSession по session_key → рвётся при логине = AN-031); оба пути заказа (COD+monobank) линкуются; фиксы объединить с AN-031 → `audit_report_section2_analytics.md`
- [x] **AN-031. КРИТИЧНО: смена session_key при логине рвёт связку.** ✅ Аудит 05.07.2026: подтверждён кодом — `login()` вызывает `cycle_key`, данные сессии переживают логин, но `UTMSession.session_key` не мигрируется; lookup по новому ключу даёт `DoesNotExist` → `audit_report_section2_analytics.md`. Что: искать/перепривязывать по `visitor_id` и fallback на `session['utm_data']`.
 - [x] **AN-032. Нормализация utm_source.** ✅ Аудит 06.07.2026 (iter4, код): подтверждено — словаря нормализации НЕТ нигде (grep `normaliz` по utm_middleware.py/utm_tracking.py/utm_analytics.py = 0); utm_source пишется в БД как есть → фрагментация подтверждена данными от 05.07 (ig/Instagram/IGShopping/Inst_Vid). Фикс: словарь нормализации при записи в UTMTrackingMiddleware + governance-конвенция (TECH-009). Где: живая БД: 'ig' (198), 'Instagram' (128), 'IGShopping' (6), 'Inst_Vid' (10) — 4 написания одного канала; 'fb'/'fb-SiteLink'; '120233970682840302' (сырой ad id). Что: словарь нормализации при записи или на уровне отчётов; UTM governance-конвенция (TECH-009).
 - [x] **AN-033. AI-источники как отдельный канал.** ✅ Аудит 06.07.2026 (iter4, код): подтверждено — детекции AI-рефереров в трекинге НЕТ (упоминания Perplexity/ChatGPT только в seo_utils.py как SEO-комментарии); chatgpt.com-трафик (109 сессий, 3-й источник) размазан по generic-каналам. Фикс: детект chatgpt.com/perplexity.ai/gemini/claude.ai по utm и referrer → канал «AI» в отчётах (TECH-065). Где: БД: utm_source='chatgpt.com' 109 сессий + referrer chatgpt.com 18. Что: детект chatgpt.com/perplexity.ai/gemini/claude.ai по utm и referrer → канал «AI» в отчётах (TECH-065).
 - [x] **AN-034. Кэш не сбрасывает UTM.** ✅ Аудит 06.07.2026 (iter4, код): полного page-cache middleware (Fetch/UpdateCacheMiddleware) в цепочке НЕТ — UTMTrackingMiddleware (settings.py:223) стоит после Session/Auth и выполняется на каждом HTML-запросе; live-прогон CRO-050 подтвердил: сессия с `?utm_source=audit` получила sessionid/twc_vid (не cache-hit). Kill-switch: env `DISABLE_ANALYTICS=true` вырезает все 3 аналитические middleware (production_settings.py) — проверять при инцидентах. Остаток (минорный): LiteSpeed/hosting-кэш поверх Django для URL с query — косвенно опровергнут живым прогоном. Где: cache_headers.py, whitenoise, hosting-кэш (Hostsila/LiteSpeed?). Что: страница с `?utm_...` не отдаётся из кэша без прохода через UTMTrackingMiddleware; проверить, что HTML с query-параметрами — MISS или что мидлварь стоит до кэш-слоя.
- [x] **AN-035. Бот-фильтр фактически мёртв.** ✅ Аудит 05.07.2026: подтверждён механизм — `SiteSession.is_bot=True` не появляется из-за early return, а `record_user_action` пишет product_view без бот-фильтра; 34k+ product_view без SiteSession раздувают метрики → `audit_report_section2_analytics.md`. Что: TECH-063 — единый bot-detect и фильтрация UserAction.
- [x] **AN-036. increment_visit на каждый запрос без UTM.** ✅ Аудит 06.07.2026: подтверждено — SELECT+UPDATE на каждый pageview (обе ветки); visit_count = pageview-счётчик; фикс: time-window 30 мин → `audit_report_section2_analytics.md`
- [x] **AN-037. Атрибуционная модель first/last touch.** ✅ Аудит 06.07.2026: несогласованность вскрыта — session-слой перезаписывается (last touch), UTMSession игнорирует новые UTM (first touch, следствие get_or_create); заказы фактически first-touch; рекомендация first_touch_*/last_touch_* поля → `audit_report_section2_analytics.md`
- [x] **AN-038. Отчётность UTM в админке.** ✅ Аудит 07.07.2026 (код-слой): проводка реальна — `?section=dispatcher` → admin.py:2828 → utm_analytics/utm_cohort_analysis → partials/admin_dispatcher_section.html; DRF `/api/utm/*` (18 actions) под IsAdminUser, CSV под staff_member_required — права закрыты; **P1 (следствие CRO-041/042, своего бага нет): conversions/revenue/ROI/LTV/когорты = системные нули (is_converted 0/1015, Order.utm_session FK 0/41), достоверны только sessions/geo/devices**; P2 — N+1: 2–3 запроса на строку × ~10 блоков = сотни запросов на рендер вкладки; P2 — нет нормализации utm_source (ig 198 vs Instagram 128 — разные строки); P3 — unique_visitors по distinct IP; P3 — revenue только payment_status='paid' (доставленный COD выпадет); P3 — исключения глушатся в нулевой контекст. Остаток: браузерная проверка вкладки/экспорта + обязательная пересверка после TECH-060/061 → `audit_report_section2_analytics.md`
- [x] **AN-039. Событие search и PII.** ✅ Аудит 06.07.2026 (код-слой): сырой query пишется без обрезки/маскировки; риск низкий (товарный поиск), фикс-однострочник описан; остаток — выборка 181 записи на PII по SSH → `audit_report_section2_analytics.md`
 - [x] **AN-040. GA-Data API интеграция.** ✅ Аудит 06.07.2026 (iter4, код): ИСПОЛЬЗУЕТСЯ — `external_analytics.py` импортируется из `admin_analytics.py` (админ-дашборд); обвязка качественная (GA4 TTL 30мин, Clarity дневной бюджет 10 req, SWR 30 дней, backoff 1ч) — НЕ кандидат на удаление. Ключа сервис-аккаунта в git НЕТ (git ls-files чисто). **Риск NEW-405: авто-дискавери `*service*account*.json` в каталоге проекта — проверить на сервере наличие такого файла в webroot (батч iter4 §6).** Где: requirements (google-analytics-data), `storefront/services/external_analytics.py`. Что: используется ли реально; ключ сервис-аккаунта не в репозитории; если не используется — кандидат на удаление.

### 2.5 Consent и приватность

- [x] **AN-050. Cookie-consent баннер.** ✅ Аудит 06.07.2026: баннера НЕТ вообще (grep по всем шаблонам/JS = 0), Consent Mode v2 не настроен (`gtag('consent'` = 0), пиксели грузятся безусловно (interaction-триггер GTM — перф-механизм, не consent); для UA-рынка не блокер, для ЕС-трафика — формальное нарушение GDPR/ePrivacy → TECH-077 (гео-баннер для не-UA + Consent Mode default denied для ЕЭЗ) → `audit_report_section2_analytics.md` Где: base.html, шаблоны partials. Что: есть ли баннер вообще; пиксели грузятся до согласия? (TECH-077); consent mode в GTM.
- [x] **AN-051. IP и ��еолокация — законность хранения.** ✅ Аудит 06.07.2026: сбор шире задокументированного — UTMSession хранит IP+country/city/region/timezone (utm_middleware.py:153–192), SiteSession/PageView тоже IP+UA; политика `/privacy-policy/` (support_content.py:1134) — 2 секции общих слов, НЕ упоминает IP/гео/cookies/пиксели/сроки/права субъекта/третьих лиц; **retention нет: trim_analytics чистит только PageView/SiteSession (90д), UTMSession+UserAction — никогда (NEW-404)**; задача «data lifecycle»: текст политики привести к реальности + чистка UTM-таблиц (с DB-004) → `audit_report_section2_analytics.md` Где: utm_middleware.py (ip_address, geo), UTMSession model. Что: privacy policy упоминает сбор; retention-политика (сессии старше N месяцев — чистка).

---

## РАЗДЕЛ 3. ТЕХНИЧЕСКОЕ СОСТОЯНИЕ И ТЕХ. ДОЛГ

### 3.1 Мёртвый код и файлы

- [x] **TD-001. storefront/views.py.backup.** ✅ Аудит 06.07.2026: **КОРРЕКЦИЯ: удалять НЕЛЬЗЯ — файл исполняется в рантайме** (`_load_legacy_views` exec-ит его, whitelist 102 имени, 30 боевых маршрутов через `_legacy_view`); план миграции → `audit_report_section3_techdebt.md` Где: `twocomms/storefront/views.py.backup`. Что: удалить из репо (git rm), убедиться, что ничего его не импортирует.
- [x] **TD-002. order_success_old.html.** ✅ Аудит 06.07.2026: 0 ссылок по всему репо — безопасно удалить (P3) → `audit_report_section3_techdebt.md` Где: `twocomms_django_theme/templates/pages/order_success_old.html`. Что: grep по render/template_name — если не используется, удалить.
- [x] **TD-003. Celery-шимы и следы.** ✅ Аудит 06.07.2026: **P2-БАГ — битый импорт `send_telegram_notification_task` из storefront.tasks (символа нет) → все send_admin/personal_message синхронны в request-потоке**; ai_signals из-за shim = синхронная AI-генерация при сохранении товара; решение «Celery не возвращаем» зафиксировать → `audit_report_section3_techdebt.md` Где: `twocomms/__init__.py` (try-import celery), `orders/tasks.py`, `storefront/tasks.py` (no-op шимы), `celery.log` на сервере. Что: Celery удалён осознанно (хостинг без воркеров) — ��роверить, что ни один вызов не рассчитывает на асинхронность (feeds_queue, web_push, ai-генерация); удалить celery.log с сервера; зафиксировать решение «Celery не возвра��аем» в TECHNICAL_TASKS.
- [x] **TD-004. Мусо�� в ��ор��е twocomms/.** ✅ Аудит 06.07.2026: полная инвентаризация с верди��тами (Ideas/ ~150 md; xlsx с ценами P2; `tmp/feeds/feeds_dirty.flag` ��� раб��чий, только gitignore; 494cb…txt — верификация, проверить URL перед удалением) → `audit_report_section3_techdebt.md` Где: `494cb80b2da94b4395dbbed566ab540d.txt`, `replacement.txt`, `pricelist.html`, `wholesale_prices.xlsx`, `Оптові ціни....xlsx`, `analyze_views_migration.py`, `check_views_*.py`, `_audit*`, `Promt/`, `Ideas/`. Что: рассортировать: рабочие скрипты → `scripts/`, документы → docs, мусор → удалить; xlsx с ценами не должны лежать в публичном репо, если содержат закупочные цены.
- [x] **TD-005. Отчёты *.md в корне репозитория (100+ файлов).** ✅ Аудит 06.07.2026: подтверждено 202 md + 53 txt/sh/py; план переноса в docs/archive одним git mv-PR (риск 0) → `audit_report_section3_techdebt.md` Где: корень репо (CART_*, PERF_*, SEO_* и т.д.). Что: перенести в `docs/archive/` одним PR; в корне оставить README + активные план-файлы.
- [x] **TD-006. legacy_stubs.py.** ✅ Аудит 06.07.2026: 48 заглушек, все живы в urls; admin_store_* возвращают фейковый `{'status':'ok'}` (хуже 410); сливается с TD-001 в задачу «ликвидация legacy-слоя» (а: fix monobank quick, б: перенос живых, в: 410 мёртвым) → `audit_report_section3_techdebt.md` + `audit_report_legacy_stubs.md`
- [x] **TD-007. Дублирующиеся системы аналитики.** ✅ Аудит 06.07.2026: карта построена — tracking.py/utm_tracking.py/ai_signals АКТИВНЫ, **ab_testing.py МЁРТВ (0 импортов) — удалить**; дубль-слой PageView vs UserAction page_view = два источника правды; остаток: PageView.count() при SSH-сеансе → `audit_report_section3_techdebt.md` Где: `storefront/tracking.py` vs `utm_tracking.py` vs `ab_testing.py` vs `ai_signals.py`. Что: карта «кто что пишет и куда»; выявить неиспользуемые (0 записей в соотв. таблицах БД — проверить через SSH) и завести задачу на удаление.

### 3.2 Производительность

- [x] **TD-010. Узкие места рендера.** ✅ Аудит 06.07.2026 (код-слой): context processors здоровые (orders_processing_count кэширован 60s), cached loader включён; TTFB-замер — остаток к живому сайту → `audit_report_section3_techdebt.md`
- [x] **TD-011. Redis-кэш: покрытие и инвалидация.** ✅ Аудит 06.07.2026: **прод на production_settings.py — трёхвариантный CACHE_BACKEND (redis/locmem/file-based ДЕФОЛТ)**; ~20 точек кэша, сигналы инвалидации осмысленны; реальный бекенд — SSH-хвост → `audit_report_section3_techdebt.md`
- [x] **TD-012. REDIS_IGNORE_EXCEPTIONS маскирует падения.** ✅ Аудит 06.07.2026: подтверждено + связка: при падении Redis **rate limiting отключается молча** (fail-open в middleware); рекомендация: LOG_IGNORED_EXCEPTIONS + алерт → `audit_report_section3_techdebt.md`
- [x] **TD-013. Статика: compressor + whitenoise.** ✅ Аудит 06.07.2026: конфиг здоровый (Manifest+180d immutable), но `ensure_compress_offline` тихо переключает на on-the-fly компрессию при отсутствии manifest — проверить деплой-скрипт (SSH-хвост) → `audit_report_section3_techdebt.md`
- [x] **TD-014. Медиа-кэш мидлвари.** ✅ Аудит 06.07.2026: ImageOptimizationMiddleware зарегистрирован, но default-выключен (пустой pass-through + ThreadPoolExecutor на воркер); **MediaCacheMiddleware не зарегистрирован = мёртвый код, удалить**; размер media/optimized_cache — SSH-хвост → `audit_report_section3_techdebt.md`
- [x] **TD-015. passenger_wsgi + лимиты хостинга.** ✅ Аудит 07.07.2026 (SSH+код): 4 Passenger `lswsgi` процесса по 119–161 MB RSS + IG bot daemon/ensure по 130/127 MB; `.htaccess` задаёт только AppRoot/BaseURI/Python, лимитов воркеров нет; **важная коррекция старой гипотезы: Redis broker configured, но сервер не резолвит Redis Cloud host (`Name or service not known`), поэтому рост очереди `celery` НЕ подтверждён**; order notifications сейчас thread/sync shim, общий TelegramNotifier из-за битого import уходит в sync; celery beat survey не выполняется и cron для него отсутствует → `audit_report_section3_techdebt.md`
- [x] **TD-016. Логи на сервере.** ✅ Аудит 07.07.2026 (SSH, counts-only): `logs/` = **958 MB**, главный файл `logs/nova_poshta_cron.log` = **826,93 MB**, user logrotate/crontab-rotate не найден; `celery.log` мёртвый 127B; password hits 0, но PII-like hits есть в `django.log`/`stderr.log`/`recover_checkouts.log`/tail `nova_poshta_cron.log`, `rum.log` имеет 24 secret-like hits без вывода строк → `audit_report_section3_techdebt.md`

### 3.3 Надёжность и безопасность

- [x] **TD-020. Бэкапы MySQL.** ✅ Аудит 05.07.2026: **P0 — регулярных бэкапов НЕТ**, внедрение — исполнителю → `audit_report_section3_techdebt.md` Где: сервер (cron владельца/hostsila-панель). Что: расписание дампов; тест восстановления на копии (TECH-042); дампы не лежат в web-доступной папке.
- [x] **TD-021. Секреты не в репозитории.** ✅ Аудит 06.07.2026: текущее дерево ЧИСТОЕ (ls-files → только .env.example; SECRET_KEY/monobank/binotel — из env; токен-regex-скан = 0); **P1: в git-истории лежат удалённые `twocomms/db_config.env` (SECRET_KEY 28 симв., DB_USER/DB_NAME/HOST) и `twocomms/.env.production` (REDIS_URL 114 симв. с кредами)** — восстановимы `git show`; действия: ротация SECRET_KEY+Redis-кредов на сервере, history-rewrite только при расшаривании репо; значения секретов в отчёты не копировались → `audit_report_section3_techdebt.md` Где: весь репо.
- [x] **TD-022. Мониторинг 5xx и JS-ошибок.** ✅ Аудит 06.07.2026: подтверждено — алертинга НЕТ: django.request ERROR → stderr.log «в никуда» (mail_admins/Sentry/ADMINS = 0), глобального window.onerror/unhandledrejection нет (JS-сбои невидимы), handler500 нет; телеграм-обвязка богатая (send_admin_message и др.), но к ошибкам не подключена; план TECH-041: ERROR-Handler→TG с rate-limit + window.onerror→/api/client-error/ → `audit_report_section3_techdebt.md` Где: нет системы (проверено).
- [x] **TD-023. Rate limiting.** ✅ Аудит 06.07.2026: глобальный 100/min/IP есть, но **ключ — спуфаемый X-Forwarded-For**, fail-open при падении кэша; django-ratelimit — только 2 cart-lookup с block=False; **логин/регистрация/чекаут/отзывы — без точечных лимитов** → `audit_report_section3_techdebt.md`
- [x] **TD-024. DRF API поверхность.** ✅ Аудит 06.07.2026: карта прав построена; **`/api/analytics/track/` — публичный no-op (TODO вместо записи)**; Swagger/Redoc публичны — закрыть за staff; Cart/Communication — детальный аудит объектных прав отдельно → `audit_report_section3_techdebt.md`
- [x] **TD-025. db_routers.py.** ✅ Аудит 06.07.2026: НЕ мёртвый — опциональная изоляция dtf-БД с общим auth; код корректен, действий не требуется → `audit_report_section3_techdebt.md`

### 3.4 Статусная модель заказа (ф��ндамент точных данных)

- [x] **TD-030. Текущие статусы: только done/cancelled.** ✅ Код-слой 07.07.2026: в модели статусов ПЯТЬ (new/prep/ship/done/cancelled, models.py:11-17) + отдельная ось payment_status (prepaid = deposit_paid уже покрыт); «только done/cancelled» — свойство ДАННЫХ (НП-синк авто-переводит в done, транзитные статусы не видны без истории). Разрывы против TECH-070: **P1 — нет OrderStatusHistory/timestamps смен статусов** (только created/updated); P2 — нет refused_rts (відмова НП неотличима от cancelled); P2 — delivered/done склеены (НП «доставлено» ≠ «получено клиентом»). Аддитивная схема миграции предложена в отчёте — НЕ внедрено, ждёт согласования. Детали: `audit_report_td_orders.md`.
- [x] **TD-031. Нова Пошта API-синк статусов.** ✅ Код-слой 07.07.2026: трекинг СУЩЕСТВУЕТ и развит — `get_tracking_info` через TrackingDocument.getStatusDocuments, массовый `update_all_tracking_statuses` (обход заказов с ТТН не в done/cancelled), management command для cron + fallback-middleware (daemon-поток с cache-lock и close_old_connections). TECH-071 в части авто-shipped/delivered реализован. P2 — RTS/відмова НП не маппится ни во что (заказ зависает в ship); P2 — daemon-поток хрупок на Passenger, cron надёжнее (наличие crontab — остаток SSH); P3 — сверить какой из двух NP-middleware включён в prod. Детали: `audit_report_td_orders.md`.
- [x] **TD-032. COGS-снапшот в заказе.** ✅ Код-слой 07.07.2026: подтверждено — снапшота НЕТ. OrderItem (models.py:338-351) снапшотит только цену продажи (unit_price/line_total); полей себестоимости нет ни у OrderItem, ни у Product/ProductColorVariant. В finance/ себестоимость есть только в консигнационном контуре (`models_consignment.py:193` unit_cost на позиции), связи с OrderItem нет → маржа по retail невычислима ретроспективно. TECH-072 спроектирован в отчёте: аддитивно `OrderItem.unit_cost_snapshot` (NULL) + `cost_source` (consignment/manual/estimate), источник — консигнационный unit_cost на момент заказа. Детали: `audit_report_td_orders.md`.
- [x] **TD-033. CustomPrintLead воронка мертва.** ✅ Аудит 05.07.2026: подтверждён кодом+БД — 28/28 лидов `new`, смена статуса есть, но не используется; 0 связок с заказами → `audit_report_section3_techdebt.md`. Что: TECH-062 — статусная модель quoted/won/lost + движение из админки.

---

## РАЗДЕЛ 4. SEO И AEO

### 4.1 Техническое SEO

- [x] **SEO-001. robots.txt.** ✅ Аудит 06.07.2026 (iter4, live): корректный — закрыты /admin/, /admin-panel/, /orders/, /checkout/, /api/, /debug/, /dev/, /push/; отдельные блоки для AdsBot-Google(-Mobile); блокируются трекинг-параметры (`/*?utm_*`, gclid, fbclid, yclid, msclkid, ref, sort, order). Замечание: `Disallow: /*?utm_*` не даёт Google краулить utm-URL и увидеть их canonical — приемлемый трейд-офф, но помнить при диагностике GSC «Проиндексировано, несмотря на блокировку».
- [x] **SEO-002. Sitemap-index и секции.** ✅ Аудит 06.07.2026 (iter4, live): индекс 200, все 8 под-sitemap 200 (static=54, products=195, product-variants=178, categories=9, blog=45, color-categories=12, thematic=4, images=65 locs); выборка 6 URL из products — все 200. **Находка NEW-410: в sitemap-products только 1 `lastmod` на 195 URL — lastmod фактически отсутствует (P3).** Замечание: в sitemap-products вперемешку uk и /ru/ URL. Остаток: дубли product-variants vs canonical (выборочная сверка).
- [x] **SEO-003. Canonical и дубли.** ✅ Аудит 06.07.2026 (iter4, live): canonical есть на /, /ru/, PDP (тег многострочный — grep одной строкой его не ловит, учитывать в будущих аудитах); `/?utm_source=test` канонизируется на чистый `https://twocomms.shop/` — корректно; /ru/ канонизируется на /ru/ (self) — корректно для hreflang-кластера. Остаток: пагинация и цветовые лендинги (выборочно).
- [x] **SEO-004. hreflang uk/ru/en.** ✅ Аудит 06.07.2026 (iter4, live): на /, /catalog/, PDP, /catalog/hoodie/ кластеры uk-UA/ru-UA/en-UA + x-default взаимные и корректные; /ru/ отдаёт 200 с зеркальным кластером. Остаток: заполненность переводов в БД (name_ru/name_en) — SSH-батч.
- [x] **SEO-005. H1-иерархия.** ✅ Аудит 06.07.2026 (iter4, live): ровно один H1 на всех 4 проверенных шаблонах (главная, каталог, PDP, категория); H1 PDP = название товара (`tc-product-title`). Остаток: blog-шаблоны (выборочно).
- [x] **SEO-006. Битые внутренние ссылки и 410.** ✅ Аудит 07.07.2026: полный rate-limited краул завершён (489/489 sitemap-URL, 1 req/2.5s) → отчёт `data/seo_crawl_analysis_report.md`, сырьё `data/seo_crawl_results.jsonl`. Итог: **все 489 sitemap-страниц отдают 200 без редиректов**; 39 внутренних ссылок вне sitemap — все 200, 0 битых, 0 redirect-chains; 6/6 тестов несуществующих URL (product/catalog/blog/random) → чистый **404 без редиректов** (не 410 — P3, допустимо; кастомная 404-страница отдаётся, body ~93KB). Единственная аномалия за весь краул — 1 интермиттентный 500 на `/blog/` в ранней фазе (при повторе 200, Passenger-overload, см. SEO-010/TD-022). Вывод: внутренних 404 нет — требование выполнено; 410 для удалённых товаров не реализован (сейчас 404) — опционально TECH-задача низкого приоритета. Где: `404.html`, `410.html`, GSC (доступ у владельца).
- [x] **SEO-007. Мета-титлы/дескрипшены из БД.** ✅ Аудит 07.07.2026: полный краул 489/489 страниц → отчёт `data/seo_crawl_analysis_report.md`. База структурно чистая: **0 без title/description/canonical/OG/H1, 0 multiple-H1, 0 noindex в sitemap, canonical == final URL на 100% страниц**. Замечания по качеству (P3, полные списки в отчёте): **8 title <30** (/ru|en/delivery/, /doglyad-za-odyagom/, /blog/ локали); **41 title >65** (в осн. product-варианты цвет+фит: where-mi-present, pojuy-ts, reality-bends-future-2026 до 82 симв., + /pro-brand/, /en/cooperation/); **121 description >165** (шаблон product-description раздувается на вариантах; + /custom-print/, /wholesale/ до 204); **6 description <70** (/blog/category/news/ 13–16 симв. — фактически пустышки); **дубли title/description: 13 групп** — из них 2 реальные кросс-товарные (ru- и en-версии трёх разных товаров Reality Bends шарят один title «Футболка "Reality Bends"…» — генератор мета не различает товары), остальные 11 — en-версии блог-постов с непереведёнными украинскими title (пробел локализации, hreflang должен спасать, но title стоит перевести). Что делать (исполнителю): поджать шаблон product title/description (лимит 60/160), уникализировать Reality Bends x3 (ru+en), заполнить description категорий блога, перевести en-титлы блога; батчами по 10 стр. с GSC-контролем (RISK-13). Где: `storefront/seo_utils.py`, `services/product_seo_autofill.py`, `product_seo_block.py`, `category_seo_blocks.py`; БД (meta-поля Product/Category).
  **Post-fix 16.07.2026 (F-008):** static commercial description outliers из SEO-007 закрыты `7fa568b1`; live 12/12 UK/RU/EN значений для cooperation/custom-print/wholesale/catalog имеют длину 120–160. Product/blog outliers исторического краула остаются отдельной задачей.
  **Post-fix 16.07.2026 (F-013):** category title/H1 «strategy mismatch» оказался производным дублем F-001. `e2558396` устранил обрезанные DB titles; свежие live `tshirts`/`hoodie`/`long-sleeve` UK/RU/EN дают 9/9 HTTP 200, полные title 44–57 и H1 45–73 с единым intent. Разные длины/формулировки полей являются штатной SEO-архитектурой, новый код не нужен.
  **Post-fix 16.07.2026 (F-028):** `da910c46` исправил runtime locale propagation в PDP merchandising, variants API, quick view и Product JSON-LD. Local/server: focused 16/16, integer API routes 7/7, Django check clean; `origin/main` и server HEAD = `da910c469fd91b8b5bb3535890e74ad9acf384b4`, Passenger restart, health 200. Live 13 SKU × 3 локали = 39/39 согласованы по HTTP/title base/H1/variant data/Product JSON-LD; RU/EN API корректны, quick-view/images 200. Миграций/data mutation не было. Статус [o] PARTIAL: owner-approved cross-locale slug-family naming policy остаётся; автономный rename не выполнялся.
- [x] **SEO-008. Google Merchant feed.** ✅ Аудит 07.07.2026 (код + live): фид ДИНАМИЧЕСКИЙ (view static_pages.py:1173, 384 оффера/65 групп, HTTP 200, все обязательные поля 384/384, картинки живые); статический xml в корне — пустой мёртвый артефакт; **P1: все 384 `<g:link>` дают 301 с потерей `color` (риск disapproval «mismatched landing page»)**; P2: availability захардкожен in_stock; P3: gtin 0/384 без identifier_exists, shipping не задан, 4 дубль-URL фида, нет кэша ответа (~3s CPU/GET) → `audit_report_section4_seo.md` Где: `google_merchant_feed.xml` (корень twocomms/ — статический файл!), `storefront/feeds.py`, `services/marketplace_feeds.py`, `AUTO_GOOGLE_MERCHANT_FEED_UPDATE.md`. Что: ф��д генерируется автоматически или лежит устаревший статикой; цены/наличие в фиде = БД; фид отдаётся по URL и принят в Merchant Center.
- [x] **SEO-009. IndexNow и Google Indexing.** ✅ Аудит 07.07.2026 (код): обе интеграции добротные (on_commit, retry, host-фильтр, audit-log с rolling-quota 24h/200 для Google, креды НЕ в git); **P2: си��нальный путь Google идёт без дедупа/quota_limit — массовое пересохранение 65 товаров = ~130 вызовов = 65% дневной квоты**; P3: сигнал без changed-fields-проверки, лишний SELECT в pre_save, API формально для JobPosting, audit-таблица без retention; остаток SSH: INDEXNOW_KEY live, json-креды на сервере → `audit_report_section4_seo.md` Где: `services/indexnow.py`, `services/google_indexing.py`. Что: ключи валидны, реально дергаются при публикации/изменении; не спамят при массовых пересохранениях.
- [x] **SEO-010. Скорость как ранж-фактор.** ✅ Аудит 07.07.2026 (live-замеры agent-browser, эмуляция iPhone 14): CWV НЕ зелёные — LCP главная 4.8s / каталог 16.4s / карточка 2.5s (CLS 0.0 везде — фронт хороший); корень СЕРВЕРНЫЙ: `Vary: Cookie` + `Set-Cookie` (csrftoken+twc_vid) на каждом анонимном GET полностью выключают LiteSpeed page cache → каждый визит = полный Django-рендер; TTFB бимодальный (тёплый 0.5–1.0s vs холодный 8–18s, Passenger cold-start/очередь) + интермиттентные 503 (1/12 запросов). P1-фикс: убрать csrftoken/twc_vid с анонимных GET или настроить cache-vary ignore. Остаток: CrUX/GSC поле (владелец), панель LiteSpeed Hostsila. Детали: `audit_report_section4_seo.md` Где: см. CRO-003; каталог и карточки тоже. Что: CWV зелёные на 3 ключевых шаблонах mobile (TECH-040).
- [o] **F-035 / TECH-082. CSP reporting и pixel/GTM policy.** ✅ Follow-up 16.07.2026: `341d42a9` исправил и production-verified modern/legacy receiver + isolated rotating privacy-safe JSON log; local/server 16/16, check/compile clean, live valid/malformed 204, `/csp.log` 404, logger WARNING + `propagate=False`. Исторические ~13 bare сообщений сохранены; current pre-fix baseline = 588. Live header остаётся 13-directive с ожидаемыми origins, F-041 PASS и allowlist/header не менялись. Статус `[o] PARTIAL`: real post-deploy reports = 0, поэтому наблюдать >=24h от 2026-07-16 16:07:50 UTC и менять policy только по подтверждённому deployed/GTM origin → `audit_report_section4_seo.md`.

### 4.2 Structured Data (Schema.org)

- [x] **SEO-020. КРИТИЧНО: Product schema на карточке.** ✅ ОПРОВЕРГНУТО 06.07.2026 (iter4, live view-source PDP): JSON-LD НА КАРТ��ЧКЕ ЕСТЬ — 5 бл��ков `application/ld+json`, включая Product + Offer + Brand + OfferShippingDetails + MerchantReturnPolicy + BreadcrumbList + FAQPage (13 Q&A) — рендерится из `seo_utils.py`, а не ��з шаблона (поэтому grep по шаблону не нашёл). TECH-030 НЕ нужен. Замечания: (1) NEW-411: site-wide Organization-граф включает `subjectOf: NewsArticle` c publisher `GovernmentOrganization` (Мінветеранів, seo_utils.py:1518-1532) — вшит на каждую страницу, включая PDP: bloat + сомнительная типизация (мин-во не «publisher» статьи бренда) — P3; (2) прогнать Rich Results Test вручную для подтверждения валидности (ручной шаг).
- [x] **SEO-021. Review/AggregateRating schema.** ✅ Аудит 07.07.2026 (код + live): реализовано дисциплинированно — единый источник истины (reviews/services/aggregate.py, только APPROVED, порог ≥1), aggregateRating+top-5 Review в схеме только при show_rating, хардкодов рейтингов НЕТ, ProductGroup намеренно без review (фикс GSC), тесты есть; live: 0 отзывов → схема честно молчит (корректно); P3: мёртвый product_detail_new.html без review_summary, +1 SQL в генераторе → `audit_report_section4_seo.md` Где: product_detail.html, reviews. Что: после появления отзывов — валидная разметка (TECH-031); НЕ размечать фейковые рейтинги.
- [x] **SEO-022. Организация/сайт schema.** ✅ Аудит 07.07.2026 (код-слой): выполнено — Organization глобально через `{% organization_schema %}` в base.html:902 (единый источник StructuredDataGenerator/seo_utils, стабильный `@id`, logo ImageObject), рядом website_schema (WebSite+SearchAction) и founder_schema; BreadcrumbList есть на каталоге (unified @graph), карточке, индексе и контактах; inline-дублей Organization в шаблонах нет (вычищены, cooperation.html — коммент-заглу��ка); **P3 — sameAs содержит только Instagram+Telegram, TikTok отсутствует (нужен подтверждённый handle от владельца — фикс одна строка в seo_utils)**. Остаток: Rich Results Test владел��цем → `audit_report_section4_seo.md`
- [x] **SEO-023. FAQPage schema.** ✅ Аудит 07.07.2026 (код-слой): выполнено — единый тег `{% faq_schema %}` (seo_tags.py:377) подключён на 6 страницах (cooperation, custom_print, pro_brand, product_detail, support_page, wholesale) + PDP получает FAQPage (13 Q&A) через seo_utils; требование TECH-032 «расширить на 5+ страниц» фактически ЗАКРЫТО; соответствие видимому контенту обеспечено конструктивно — разметка и HTML рендерятся из одного faq_items; при пустых items разметка не эмитится. Остаток: выборочный Rich Results Test + проверить, нет ли дублирования FAQPage карточка↔seo_utils на одной странице → `audit_report_section4_seo.md`

### 4.3 AEO (Answer Engine Optimization)

- [x] **AEO-001. AI-трафик уже идёт — усилить.** ✅ Аудит 07.07.2026 (SSH/БД): `utm_source=chatgpt.com` уже **119 сессий** + 19 referrer hits; топ landing pages: `/` 52, `/catalog/` 13, `/en/catalog/tshirts/` 10, `/catalog/tshirts/` 10, `/en/catalog/` 5, `/ru/catalog/` 4; mobile 100/119, converted=0 из-за общей UTM-поломки; усилять главную/каталог/hoodie/long-sleeve/custom-print/pro-brand фактологией, отдельно убрать AI-перекос в tshirts → `audit_report_section4_seo.md`
- [x] **AEO-002. llms.txt.** ✅ Аудит 06.07.2026 (iter5, live): `/llms.txt` (200) и `/llms-full.txt` (200) уже существуют и качественные — канонические маршруты, Brand facts (Founded: 2022, Kharkiv, founder story c ссылкой на mva.gov.ua), Commerce facts (цены/доставка/возврат/лояльность), правила цитирования для AI; robots.txt не противоречит; код: `twocomms/urls.py:62-64` + `views/static_pages.py` + регресс-тесты в `test_seo_regressions.py`. Создавать ничего не нужно — TECH-035 фактически выполнен ранее.
- [x] **AEO-003. Q&A-структура контента.** ✅ Аудит 06.07.2026 (iter5, код+live): Q&A-инфраструктура ХОРОШАЯ — PDP: `tc-size-table` HTML-таблица замеров + per-product FAQPage JSON-LD (только при curated `product.faqs`, дедуп с глобальным /faq/ — задокументировано в шаблоне); live /faq/ = 31 вхождение FAQPage/acceptedAnswer; /rozmirna-sitka/ = 2 HTML-таблицы (не картинки); сроки отправки «1–3 робочі дні» прямо в PDP benefit-блоке; FAQPage также в blog_blocks/category_color_landing/product_seo_landing. Остатки (не блокеры, связаны с другими пунктами): лонгсливы без size-таблицы (= CRO-023), «состав» в product_copy_v2 упоминается лишь 5 раз — прямой ответ «Из чего?» в первом абзаце описаний не системный (P3, кандидат при переписывании near-duplicate из CRO-021).
- [x] **AEO-004. Тон бренда во всех текстах.** ✅ Аудит 06.07.2026 (iter5, grep-аудит кода): «бейсбол/baseball» — 0; от-первого-лица владельца — 0 (все совпадения «мені» = название принта «мені поЖуй», легитимно); БД-описания уже проверены в CRO-021 (65/65 чистые). Список на переписывание (2 кандидата, P3): `_product_themes.py:116` («спортсмени, музиканти, бійці» в аудитории last breath) и `:120` («спорт, виступи, дедлайни, бій») — слово «спорт» в перечислениях, не спорт-эстетика; `custom_print.html:978` («спортивна команда» как B2B-кейс) — легитимно, не трогать. Blog-шаблонов с нарушениями нет.
- [x] **AEO-005. «С 2014» и ложные факты.** ✅ Аудит 06.07.2026 (iter5): ложного «с 2014» в истории бренда Н��Т — repo-grep («2014» по html/py/js/txt всех app-директорий) и live-скан 9 ключевых страниц (/, pro-brand, faq, delivery, contacts, custom-print, wholesale, cooperation, blog) чистые; llms.txt корректно указывает Founded: 2022. Единственное вхождение — нейтральный placeholder примера текста рукава `EST. 2014` в custom_print.html:648 → исправлен на `EST. 2022` (+ locale en/ru django.po; на сервере после деплоя нужен `manage.py compilemessages`). Остаток: grep «2014» по БД-описаниям товаров — включён в SSH-батч CRO-051.
- [x] **AEO-006. AI-автогенерация контента.** ✅ Аудит 06.07.2026 (iter5, код): риск галлюцинаций НИЗКИЙ — `product_seo_autofill.py` НЕ вызывает AI вообще (чисто шаблонная генерация, идемпотентная, факты о составе/уходе исторически выверены: hoodie 320 г/м² трьохнитка отдельно от футболок — фикс задокументирован в докстрингах); реальные OpenAI-вызовы только в `translate_products.py` (переводы RU/EN, есть `--mirror-fallback`) и `seo_utils.py` (категорийные keywords/descriptions, graceful degradation без ключа). Тон шаблонов соответствует бренду («мілітарно-streetwear ДНК», подтримка ЗСУ). Замечание P3: шаблон autofill пишет «мілітарний» для ВСЕХ категорий — для чисто streetwear-принтов формально неточно, вычитка выборочно. Остаток: `ai_generation.log` на сервере — включён в SSH-батч.

---

## РАЗДЕЛ 5. БАЗА ДАННЫХ MYSQL �� ЦЕЛОСТНОСТЬ

- [x] **DB-001. Медленные запросы.** ✅ Аудит 07.07.2026 (SSH/MySQL): `slow_query_log=OFF`, `long_query_time=10`, `log_queries_not_using_indexes=OFF`, `Slow_queries=1633`; топ-10 SQL сейчас невозможно получить без controlled slow-log/performance sampling; кандидаты остаются DB-005 (`utm_cohort_analysis` N+1), analytics JOIN UserAction/SiteSession, catalog cold-start, management lead parsing → `audit_report_section5_db.md`
- [x] **DB-002. Индексы под аналитические запросы.** ✅ Код-слой 07.07.2026: покрытие в целом хорошее (UserAction: 4 составных индекса; session_key везде unique+indexed). P2 — у UserAction нет is_bot, бот-фильтр отчётов = JOIN к SiteSession с OR-isnull (admin_analytics.py:551) → индекс не работает, денормализовать is_bot или UNION; P2 — нет `(site_session, action_type)` под воронку по сессиям; P3 — UTMSession перегружен 20+ одиночными db_index (write-amplification на shared MySQL). Остаток SSH: EXPLAIN на живой MySQL. Детали: `audit_report_section5_db.md`.
- [x] **DB-003. Целостность Order ↔ UTMSession ↔ User.** ✅ Аудит 07.07.2026 (SSH/БД): `Order` = 43, **0/43 с `utm_session`**, **0/43 с `utm_source`**, `UTMSession.is_converted=True` = 0; `UserAction` имеет 6 distinct `order_id`, из них orphan ids **259, 260** без Order → `audit_report_section5_db.md`
- [x] **DB-004. Рост UserAction (36 859 строк).** ✅ Аудит 07.07.2026 (SSH/SHOW TABLE STATUS): `storefront_useraction` ≈ **41 377 rows / 11,64 MB**, Engine **MyISAM**, oldest 2025-12-05, older_than_12m=0; главный вес БД не UserAction, а `management_leadparsingresult` **492 MB** и `storefront_pageview` **63,65 MB**; нужна retention-политика для всех аналитических raw-таблиц → `audit_report_section5_db.md`
- [x] **DB-005. N+1 в админ-отчётах.** ✅ Код-слой 07.07.2026: `admin_analytics.py` (1703 стр.) чистый — везде select_related/prefetch/annotate/Exists (стр. 461–585), N+1 нет. **P2 — `utm_cohort_analysis.py`: 3 классических N+1**: cohort_retention (вложенный цикл когорты×периоды, ~300–400 SQL на рендер, стр. 102–186), repeat_purchase (запрос на каждого покупателя, стр. 329–360), campaign_variants (запрос на каждый utm_content, стр. 407–425); + построчный iterator() по всем UTMSession (стр. 87). Фикс: TruncMonth+annotate/GROUP BY. Замер «<3s» — остаток SSH. Детали: `audit_report_section5_db.md`.
- [x] **DB-006. Charset/collation.** ✅ Аудит 07.07.2026 (SSH/information_schema): database charset `utf8mb4`, collation `utf8mb4_unicode_ci`; 230 таблиц все `utf8mb4_unicode_ci`; non-utf8mb4 text columns = **0** → `audit_report_section5_db.md`
- [x] **DB-007. Миграции синхронны с БД.** ✅ Аудит 07.07.2026 (SSH): `python manage.py makemigrations --check --dry-run` → **No changes detected**; unapplied migrations отсутствуют, unapplied count = **0** → `audit_report_section5_db.md`
- [x] **DB-008. PyMySQL vs mysqlclient.** ✅ Код-слой 07.07.2026: PyMySQL==1.1.2 (requirements.txt:12, комментарий «альтернатива mysqlclient»), `pymysql.install_as_MySQLdb()`-shim активен в settings.py:21 и production_settings.py:61. Вердикт: осознанный компромисс под shared-хостинг (нет toolchain для C-драйвера); P3 — задокументировать явно и заменить на mysqlclient при переезде; сохранён продуманный фикс CONN_MAX_AGE под wait_timeout=60. Детали: `audit_report_section5_db.md`.
- [x] **DB-009. Транзакционность заказа.** ✅ Код-слой 07.07.2026: Order+OrderItem атомарны в обоих путях (checkout.py:149, monobank.py:539). «Снятие остатков» НЕ существует — полей stock у Product/варианта нет, oversell не контролируется (модель «под заказ», P2 — согласовать с владельцем). **P1 — HTTP-вызов Monobank invoice/create (monobank.py:~843) выполняется ВНУТРИ transaction.atomic()** — держит соединение/транзакцию на время сетевого вызова при wait_timeout=60 (тот же анти-паттерн, что AN-011); P2 — PromoCode.usage не инкрементируется («Increment usage? Maybe later», checkout.py:246) — безлимитное конкурентное применение. Позитив: status_management/nova_poshta_service используют select_for_update. Детали: `audit_report_section5_db.md`.
- [x] **DB-010. Дропшип/опт-контуры.** ✅ Код-слой 07.07.2026: DropshipperOrder — отдельная модель (не Order) со своими item/статусами/индексами → retail-аналитика НЕ загрязняется; выплаты/статистика целостны (get_or_create + агрегация, детерминированный calculate_dropshipper_payment); пользовательские эндпоинты корректно скоупятся dropshipper=request.user (IDOR нет). P2 — admin_update_dropship_status меняет статус без select_for_update → гонка с monobank-вебхуком дропшипа (dropshipper_views.py:1302 vs 1227); P3 — order_source/sale_source свободный текст. Детали: `audit_report_section5_db.md`.

---

## РАЗДЕЛ 6. КОДОВАЯ БАЗА — ГЛУБОКИЙ АУДИТ (ИТЕРАЦИЯ 2)

### 6.1 Гигиена репозитория

- [x] **CB-001. Артефакты и скриншоты в git (132 файла).** ✅ Аудит 07.07.2026: фактически 145 tracked-файлов ≈170MB (tmp/ 101MB, artifacts/ 26MB, output/ 26MB); me.JPG в 2 копиях (вкл. twocomms/static/BrandDNA — попадает в collectstatic!); runtime-зависимостей 0; план git rm --cached → `audit_report_section6_codebase.md` Где: `artifacts/`, `output/`, `tmp/`, `opros/win-*.png`, `newCatalog/*.png`, `BrandDNA/me.JPG` (личное фото!). Что: `git rm -r --cached` + добавить в .gitignore; репо 328MB → цель < 100MB. НЕ трогать `git filter-repo` без согласования (перепишет историю — сломает клоны у всех).
- [x] **CB-002. 202 md-отчёта в корне.** ✅ Аудит 07.07.2026: фактически 175 md ~3.9MB; ссылок из кода 0 — перенос безопасен; docs/ существует → `audit_report_section6_codebase.md` Где: корень репо (`CART_*`, `META_PIXEL_*`, `GTM_*`, `MODAL_*` и т.д.). Что: одним PR перенести в `docs/archive/2025/` и `docs/archive/2026/`; в корне оставить README.md, DEPLOY.md, CHANGELOG.md. Перед переносом grep по коду на ссылки на эти файлы (их не должно быть).
- [x] **CB-003. 49 loose-скриптов в корне.** ✅ Аудит 07.07.2026 + ⚠️ P0: deploy_finance.sh содер��ит НЕзамаскированный SSH-пароль прода (единственный, остальные зачищены) → СМЕНИТЬ ПАРОЛЬ; 65 скриптов, cron их не вызывает (подтверждено CB-044), код тоже → `audit_report_section6_codebase.md` Где: корень: `fix_all.py`, `fix_ui.py`, `fix_telegram.py`, `fix_fabrics.py`, `generate_assets*.py`, `analyze_*.py`, `check_*.py/.sh`, `crawl_all_pages.py`, `optimize_images.py`, `deploy*.sh` (6 штук!), `deploy_paramiko.py`. Что: инвентаризация «жив/мёртв»: (а) какой deploy-скрипт реально используетс�� (сверит�� с процессом владельца), остальные → `scripts/archive/`; (б) одноразовые fix_* — удалить; (в) полезные (crawl, optimize_images) → `scripts/` с README. ВАЖНО: сначала проверить crontab на сервере — некоторые могут дергаться по cron (SSH-проверка обязательна до удаления).
- [x] **CB-004. Backup-файлы под git.** ✅ Аудит 06.07.2026: ⚠️ КРИТИЧЕСКАЯ НАХОДКА — `views.py.backup` НЕ мёртвый файл: `storefront/views/__init__.py:329-354` (`_load_legacy_views`) грузит его через SourceFileLoader как `storefront.legacy_views_backup` для legacy-маршрутов админки (/admin-panel/product/new/ и др.). Удалять НЕЛЬЗЯ без миграции этих view в нормальные модули. Остальные (styles.css.bak2 445KB, order_success_old.html, tmp_old_index.html) — ссылок в коде нет, можно git rm. Где: `twocomms/storefront/views.py.backup` (7790 строк), `static/css/styles.css.bak2` (445KB), `pages/order_success_old.html`, `tmp_old_index.html`. Что: grep на импорты/include каждого → git rm. Дублирует TD-001/TD-002 — закрыть одним PR.
- [x] **CB-005. xlsx с закупочными ценами в публичном репо.** ✅ Аудит 07.07.2026: 2 xlsx tracked, runtime не нужны — /pricelist_opt.xlsx генерится на лету из БД (live 200 подтверждён); ВАЖНО: views.py.backup — живой рантайм-код (_load_legacy_views), НЕ удалять → `audit_report_section6_codebase.md` Где: `twocomms/wholesale_prices.xlsx`, `twocomms/Оптові ціни*.xlsx`, `pricelist.html`. Что: если репо публичный или станет публичным — коммерчес��ая утечка; вынести из git, хранить в приватном хранилище.
- [x] **CB-006. .gitignore аудит.** ✅ Аудит 07.07.2026: секреты покрыты образцово (вкл. следы майнер-инцидента); дыры: artifacts/, output/, tmp/, opros/, newCatalog/, *.xlsx, *.bak2; исключение !views.py.backup — осознанное, оставить → `audit_report_section6_codebase.md` Где: `.gitignore` (3.9KB). Что: покрывает ли `artifacts/`, `output/`, `tmp/`, `*.bak*`, `*.backup`, `*.log`, `*.xlsx`; `.env*` игнорируется (подтверждено: .env.development.local не в индексе) — оставить как есть.
- [x] **CB-007. Каталоги-сироты.** ✅ Аудит 07.07.2026: Promt/ не tracked; 7 AI-конфигов = 69 файлов ~1.1MB (живой похоже только .kiro); .superpowers в gitignore, но 5 файлов уже tracked; код не ссылается ни на один каталог → `audit_report_section6_codebase.md` Где: `Promt/`, `Ideas/`, `opros/`, `newCatalog/`, `BrandDNA/`, `.claude/`, `.codex/`, `.cursor/`, `.kiro/`, `.serena/`, `.zenflow/`, `.superpowers/` (конфиги 7 разных AI-инструментов). Что: решить владельцем, какие AI-конфиги живы; остальное удалить или в docs.

### 6.2 Мёртвый и дублирующийся код

- [x] **CB-010. Кандидаты в мёртвые сервисы.** ✅ Аудит 06.07.2026: ОПРОВЕРГНУТО — `product_copy_v2` НЕ мёртв: импортируется из `services/product_seo_landing.py:35`, который вызывается из боевого `storefront/views/product.py:532` (build_landing) и `services/marketplace_feeds.py:1093`. Перемещать/удалять нельзя. Где: `storefront/services/product_copy_v2.py`, `services/color_seo_copy.py` — из боевого кода их использует только management-команда `recraft_product_seo.py` и офлайн-скрипты `scripts/fill_translations.py`. Что: подтвердить, что не импортируются из views/signals; пометить как offline-tooling (переместить в scripts/) либо удалить.
- [x] **CB-011. scripts/ внутри twocomms.** ✅ Аудит 07.07.2026: Phase 17 НЕ завершена (ru/en ~26% msgstr пусто, .mo свежекоммичены 07.07) — fill_translations.py + compile_mo_polib.py + list_untranslated.py живые, НЕ архивировать; wrap_themes_lazy.py УЖЕ применён (205 `_(...)` в _product_themes.py) — destructive, в архив с пометкой; 4 разовых скрипта (check_pdp_overlap, list_colors, list_color_pairs, render_social_previews) — кандидаты в архив → `audit_report_section6_codebase.md` Где: `twocomms/scripts/fill_translations.py` (**3706 строк** — 2-й по величине файл проекта!), `wrap_themes_lazy.py`. Что: это одноразовые фазовые скрипты (Phase 17) — проверить, завершена ли фаза; если да — в архив.
- [x] **CB-012. Дублирование настроек.** ✅ Аудит 05.07.2026: боевой = `twocomms.production_settings` → `audit_report_section6_codebase.md` Где: `twocomms/settings.py` (1000+ строк) vs `twocomms/production_settings.py` (переопределяет DEBUG/SECRET_KEY/логирование). Что: выяснить, какой модуль реально указан в `passenger_wsgi.py`/env `DJANGO_SETTINGS_MODULE` на сервере; двойная точка правды = риск «поправили не тот файл»; свести к settings.py + env-переменным.
- [x] **CB-013. Дублирующиеся static-директории img/ и images/.** ✅ Аудит 07.07.2026: НЕ дубли — 3 директории с непересекающимся контентом (theme/img 12MB основная, theme/images 6.3MB только banksy+hero_dtf, корневой static/img только price.png); мёртвые файл��: logo_fire/stena.png 2.7MB + hero_dtf_graphic.png (0 ссылок); stena2.png 2.7MB и configurator/ui/*.png (450-650KB) без webp; RISK-14 проверен — внешние потребители (og:image social-preview*, GMC placeholder.jpg) сидят только в img/, их пути не трогать; слияние images/→img/ безопасно (3 ссылки в pro_brand.html) → `audit_report_section6_codebase.md` Где: `static/img/` (12MB), `static/images/` (6.3MB). Что: карта использования (grep по шаблонам/CSS на `/static/img/` vs `/static/images/`); свести к одной; удалить неиспользуемые исходники (для многих PNG уже есть webp-версии рядом — PNG-оригиналы, вероятно, не отдаются).
- [x] **CB-014. dtf переопределяет collectstatic.** ✅ Аудит 07.07.2026: override подтверждён (DtfConfig перед staticfiles); функционально деплой других приложений НЕ ломает (super().handle() всегда), но P2 — hard-fail всего collectstatic при отсутствии/ошибке любого из 3 dtf-исходников; парадокс: сгенерированные .min-файлы шаблонами dtf НЕ подключаются (base.html грузит dtf.css/dtf.js без .min), whitenoise CompressedManifest и так сжимает; есть дублирующая явная команда minify_dtf_assets → план: переименовать в collectstatic_dtf или удалить override → `audit_report_section6_codebase.md` Где: `dtf/management/commands/collectstatic.py` + комментарий в INSTALLED_APPS «DTF app first to override collectstatic». Что: глобальный побочный эффект: ЛЮБОЙ collectstatic проходит через dtf-логику; проверить, что это не ломает деплой други�� приложений и задокументировать; рассмотреть переименование команды в `collectstatic_dtf`.
- [x] **CB-015. Мёртвые management-команды.** ✅ Аудит 07.07.2026 (code-часть): полная карта **93 команд** → `cb015_management_commands_map.md`. Итог: 4 RUNTIME (вызываются из signals/services/templatetags — не трогать: regenerate_feeds_if_dirty, optimize_images, reindex_indexnow, import_product_translations), 27 TESTED (включая `parser_recovery_dry_run` — он покрыт тестом management/tests.py, НЕ мёртвый, гипотеза чек-листа опровергнута), ~16 CRON?-кандидатов, ~42 OPS. **DEAD?-кандидаты: finance_seed_demo, notify_test_shops, send_storage_test, send_test_receipt + подозрительный теневой оверрайд `dtf/collectstatic`** (перекрывает стандартную django-команду!). SSH-остаток: `crontab -l` для финального вердикта по CRON?-группе (порт фильтруется из sandbox). Где: 93 команды в `*/management/commands/`. Что: сверить с crontab сервера; таблица «коман��а → где вызывается → вердикт» составлена.
- [x] **CB-016. Пакеты-призраки в requirements.** ✅ Аудит 06.07.2026: ОПРОВЕРГНУТО — оба «призрака» реально используются: `django-ratelimit` импортируется в `storefront/views/cart.py:19` (`from django_ratelimit.decorators import ratelimit`); `google-analytics-data` — lazy import в `storefront/services/external_analytics.py:277` (BetaAnalyticsDataClient). НЕ удалять. TD-023/AN-040 в этой части неверны. Где: `requirements.txt` (81 строка). Что: для каждого пакета grep на импорт: кандидаты на неиспользуемые (django-ratelimit — TD-023 подтверждает 0 использований; google-analytics-data — AN-040). Удалять только после grep по ВСЕМУ коду включая scripts/.

### 6.3 Качество кода (обработка ошибок, логирование, размер)

- [x] **CB-020. 697 широких except.** ✅ Аудит 07.07.2026: актуально уже **734** (долг растёт); в 8 денежных файлах 47 SILENT из 120; топ-20 ранжирован с приоритетами; **P1-системный паттерн: `save(update_fields)` → молчаливый fallback `save()` в 4 местах (utils.py:542/573/723 — webhook Monobank; nova_poshta_service.py:515 — флаг CAPI purchase_sent) = риск lost-update и повторного CAPI Purchase**; P1: checkout.py:233 глотает цену кастома (исполняемое подтверждение CRO-034 «кастом бесплатно»); P2: str(e) утекае�� клиенту (cart.py:943, monobank.py:288), вебхук-JSON-parse без логов (monobank.py:1342) → `audit_report_section6_codebase.md` Где: весь Python-код (grep `except Exception`/`except:`). Что: НЕ чинить массово. Приоритетно исправить в местах, где глотаются ошибки денег/данных: `orders/` (чекаут, вебхуки, CAPI), `storefront/views/checkout*.py`, `monobank.py` — каждый except должен минимум `logger.exception(...)`. Составить топ-20 самых опасных мест.
- [x] **CB-021. 120 print() в бою.** ✅ Аудит 06.07.2026: подтверждено ровно 120 print() (без management-команд/миграций). Топ: `orders/telegram_notifications.py` — 47, `accounts/telegram_bot.py` — 46, `orders/dropshipper_views.py` — 22, `accounts/telegram_views.py` — 5. Замена на logger — 4 файла покрывают 100/120. Где: grep `^\s*print(` по storefront/orders/accounts/twocomms. Что: заменить на `logger.debug/info`; print под Passenger попадает в stderr-лог хостинга без ротации.
- [x] **CB-022. God-files: план декомпозиции.** ✅ Аудит 07.07.2026: план зафиксирован для всех 6 файлов — карта доменов по фактическим def/class (management/views.py = 5 доменов, вкл. `admin_overview` ~600 строк одной функцией; management/models.py = 47 моделей → 7 доменных пакетов; admin.py резать по контекст-билдерам; **cart.py split (custom_cart.py) одновременно закрывает рекомендацию CRO-034**; static_pages.py → seo_endpoints+security_endpoints); инварианты: пустой makemigrations-дифф, НЕ трогать `_load_legacy_views` (views.py.backup — живой рантайм), порядок PR cart→admin→mgmt-models→mgmt-views→storefront-models → `audit_report_section6_codebase.md` Где: `management/views.py` 8188, `management/models.py` 3985, `storefront/views/admin.py` 3270, `storefront/models.py` 2935, `storefront/views/cart.py` 1850, `storefront/views/static_pages.py` 1950. Что: НЕ рефакторить сейчас; зафиксировать план: выделение по доменам (views/admin.py → admin_products.py + admin_orders.py + admin_analytics.py); Правило: новый код в новые модули.
- [x] **CB-023. 56 console.log в боевом JS.** ✅ Аудит 06.07.2026: подтверждено 56 console.log в 4 файлах: `analytics-loader.js`, `finance-pwa.js`, `finance-sw.js`, `ui-fallback.js`. PII в аргументах не обнаружено (payload-объекты аналитики без телефонов из форм). Где: `static/js/*.js` (grep console.log). Что: обернуть в debug-флаг или вырезать при минификации; проверить, что не логируются PII (телефоны из форм чекаута).
- [x] **CB-024. Тестовое покрытие перекошено.** ✅ Аудит 05.07.2026: баз. линия уточнена (183 файла; money-тесты в storefront ЕСТЬ, но orders/=0, accounts/=0, вебхук/CAPI/CI не покрыты); спека смок-пакета → `audit_report_section6_codebase.md` Где: 177 тест-файлов, из них почти все — finance/management; для checkout/cart/UTM — тестов почти нет. Что: минимальный набор смок-тестов на д��ньги: создание COD-заказа, monobank-вебхук (идемпотентность), слияние корзины при логине, link_order_to_utm (после фикса CRO-041). Это страховка ПЕРЕД любыми фиксами воронки.
- [x] **CB-025. 29 TODO/FIXME.** ✅ Аудит 06.07.2026: уточнено — в Python-коде (без миграций) 14 TODO/FIXME/HACK, а не 29 (остальные, вероятно, в JS/шаблонах или уже удалены). Объём мал, перенос в TECHNICAL_TASKS.md — задача на полчаса для исполнителя. Где: grep TODO/FIXME/HACK. Что: перенести содержательные в TECHNICAL_TASKS.md с ID, остальные удалить из кода.

### 6.4 Фронтенд-статика (вес страницы)

- [x] **CB-030. styles.css 565KB — монолит.** ✅ Аудит 07.07.2026: **боевой бандл = styles.purged.css 394KB, а НЕ styles.css** (styles.css — исходник для purge, в браузер не попадает); styles.base.css НЕ грузится нигде (сирота); минификация подтверждённо включена (COMPRESS_ENABLED=True, OFFLINE=not DEBUG, manifest-страж в settings.py:1024 не даст боевому подняться без сборки); полная карта подключений в отчёте; **1.1MB CSS-сирот без единой ссылки: styles.min.css 486KB + styles.direct.css 448KB + styles.base.css 119KB + critical-home.min.css 45KB + styles.css.bak2**; purge-пайплайн отсутствует в репо (невоспроизводимый артефакт) → `audit_report_section6_codebase.md` Где: `static/css/styles.css` (+ styles.base.css 119KB грузятся вместе?). Что: карта подключений по шаблонам (какие страницы что грузят); PurgeCSS-анал��з мёртвых селекторов (доля мёртвого CSS, вероятно, > 60%); минификация через django-compressor реально включена в проде? (`COMPRESS_ENABLED`, `COMPRESS_OFFLINE`).
- [x] **CB-031. Пошаговый план CSS-диеты.** ✅ Аудит 07.07.2026: план скорректирован по фактам CB-030 — (1) удалить 1.1MB сирот из git (P1, регрессия невозможна — 0 ссылок), (2) вернуть purge-пайплайн+safelist в репо (P1 — без него любой новый класс в шаблоне молча без стилей, это же RISK-05), (3) минификация УЖЕ включена — пункт закрыт проверкой, (4) critical-home.min.css НЕ рабочий critical-CSS, а сирота (актуальный механизм — инлайн cls-ultimate+perf-lite, Phase 22f) — удалить, (5) Bootstrap-subset распространи��ь с home на остальные страницы только после п.2, (6) новые страницы — только модульные CSS → `audit_report_section6_codebase.md` Где: те же файлы. Что: НЕ переписывать монолит; шаги: (1) включить/проверить минификацию, (2) убрать styles.css.bak2 из git, (3) critical-CSS уже есть (critical-home.min.css) — проверить актуальность, (4) новые страницы — только модульные CSS.
- [x] **CB-032. custom-print-configurator: 148KB JS + 160KB CSS.** ✅ Аудит 06.07.2026: OK — подключается ТОЛЬКО в `pages/custom_print.html`, в base.html глобальных подключений нет. Проблемы нет, выносить нечего. Где: static/js+css. Что: грузятся ли они ТОЛЬКО на /custom-print/ (grep по base.html/шаблонам); если глобально — вынести на страницу.
- [x] **CB-033. 16 inline-script в base.html.** ✅ Аудит 07.07.2026: 16 тегов = 8 external (все defer, ок) + 7 активных inline + 1 закомментированный (удалить вместе с partials/analytics.html); 4 блока обязаны остаться inline (CSRF self-heal, device-class детектор, sync-hints с template-логикой, trackEvent-стаб); Ahrefs-блок содержит дубль device-class детектора и стоит ВЫШЕ основного — поменять порядок; самописный GTM-loader (GTM-PRLLBF9H) сознательно гейтится от Lighthouse (таймаут 35s home) — судьба вместе с CRO-004/AN-002; блоки Ahrefs/GTM/analytics-инжектор выносимы в кэшируемый файл → `audit_report_section6_codebase.md` Где: base.html (1403 строки). Что: инвентаризация каждого блока: что делает, можно ли вынести в файл (кэшируется) или удалить; особо — самописный GTM-загрузчик (см. CRO-004/AN-002).
- [x] **CB-034. analytics-loader.js 56KB.** ✅ Аудит 07.07.2026: 1496 строк — event bridge trackEvent (Meta fbq + GA4 gtag + TikTok ttq с буферами), loaders для GA4/Clarity/Meta/TikTok, само��исный SHA-256 (80 строк — заменить на crypto.subtle), fbp/fbc cookie management; мёртвых Celery/ab_testing веток НЕТ; ⚠️ P2 связка AN-001 подтверждена: GA4 (G-109EFTWM05) грузится напрямую ПАРАЛЛЕЛЬНО GTM-контейнеру GTM-PRLLBF9H → нужно выгрузить контейнер и сверить теги (риск двойного счёта GA4/Meta/TikTok); legacy #am div с пустыми конфиг-атрибутами ({{ ga_id }} не существует в контексте); console.log TikTok-отладка в проде → `audit_report_section6_codebase.md` Где: static/js/analytics-loader.js. Что: аудит содержимого: какие пиксели/логика внутри, есть ли мёртвые ветки (Celery-эпоха, старые эксперименты ab_testing); дублирует ли GTM-теги (двойной счёт событий — связка с AN-001).
- [x] **CB-035. Шрифты 1.5MB.** ✅ Аудит 06.07.2026: состав — Inter в 4 начертаниях (Regular/Medium/SemiBold/Bold), КАЖДОЕ в двух форматах ttf+woff2. Быстрая победа: удалить .ttf-дубли (woff2 достаточно для всех современных браузеров) ≈ −50% веса без риска; далее subsetting кириллица+латиница. Где: `static/fonts/`. Что: subsetting (только кириллица+латиница), woff2-only, `font-display: swap`; удалить неиспользуемые начертания.
- [x] **CB-036. vendor/ 1.1MB.** ✅ Аудит 07.07.2026: vendor/ = только Font Awesome 6 Free self-hosted (css 104KB + woff2 308KB + ttf 700KB); дублей с CDN НЕТ (CDN грузит только Bootstrap 5.3.3); страницы без иконок уже отключают FA через block fontawesome_css; quick win — удалить 4 .ttf-fallback (−700KB, woff2 покрывает все браузеры) → `audit_report_section6_codebase.md` Где: `static/vendor/`. Что: инвентаризация: какие библиотеки, используются ли (grep подключений), нет ли дублей с CDN-подключениями из base.html.

### 6.5 Конфигурация и деплой

- [x] **CB-040. Незапиненные зависимости.** ✅ Аудит 05.07.2026: боевые версии сняты (openai==2.30.0, google-auth==2.52.0, google-analytics-data==0.22.0); пин — исполнителю → `audit_report_section6_codebase.md` Где: requirements.txt: `openai`, `google-auth`, `google-analytics-data` без `==`. Что: запинить текущие боев��е версии (узнать через `pip freeze` на сервере в ОДНОЙ SSH-сессии); мажорный апдейт openai молча сломает AI-генерацию.
- [x] **CB-041. ImageOptimizationMiddleware на shared-хостинге.** ✅ Аудит 07.07.2026 (live-проба без SSH): запрос `/media/category_icons/*.png` с `Accept: image/webp` вернул `content-type: image/png`, БЕЗ заголовка `X-Image-Cache` (middleware ставит HIT/WARMUP/MISS на каждый обработанный image-запрос) → **флаг `IMAGE_OPTIMIZATION_MIDDLEWARE_ENABLED` на бою ВЫКЛЮЧЕН (или env не задан → default=False, settings.py:849)** — runtime-оптимизация НЕ работает, RISK-10 не реализуется. `cache-control: public, max-age=2592000` отдаёт MediaCacheMiddleware (production_settings.py:56–57), не image-middleware. Код-факты: ThreadPoolExecutor(2) создаётся в `__init__` ДАЖЕ при enabled=False (лишние 2 треда/процесс — микро-утечка, P3); `IMAGE_OPTIMIZATION_ALLOW_ON_DEMAND` тоже default=False → даже при включении первого флага on-the-fly конверсия не активируется, только отдача из готового кэша. Вердикт: слой — no-op, кандидат на удаление из MIDDLEWARE (совпадает с CB-042); офлайн-оптимизация уже есть (`convert_originals_to_webp`, `compress_media_originals`, `enqueue_optimize_images` команды). SSH-остаток: подтвердить env-переменную в `~/.env` на сервере. Где: `twocomms/image_middleware.py`. Что: сколько процессов Passenger → сколько тредов суммарно; память PIL при больших PNG; включён ли флаг в проде; если да — офлайн-оптимизация по cron вместо runtime.
- [x] **CB-042. 26 middleware: порядок задокументировать.** ✅ Аудит 07.07.2026: полная таблица 26+1(prod MediaCache) слоёв с инвариантами; **RequestTrace — подтверждённый мертвец (0 потребителей `X-DTF-Debug` во всём репо) → удалять**; ImageOptimization default-off (settings.py:849) → no-op слой, убрать при подтверждении env (SSH-остаток); P3: мёртвая insert-ветка ForceHTTPS/WWWRedirect в production_settings.py:48–53 внутри `if DISABLE_ANALYTICS` (никогда не срабатывает, вводит в заблуждение); DISABLE_ANALYTICS=true выключает 3 трекинг-слоя — значение на бою проверить (стык CB-012) → `audit_report_section6_codebase.md` Где: settings.py MIDDLEWARE. Что: цепочка содержит хрупкие зависимости (комментарии «ПОСЛЕ статики!», ��ПЕРЕД SimpleAnalyticsMiddleware!»); зафиксировать инварианты порядка в комментарии-шапке; кандидаты на удаление: RequestTraceMiddleware (если X-DTF-Debug не используется), ImageOptimizationMiddleware (если выключен флагом — убрать из цепочки совсем).
- [x] **CB-043. Git-состояние сервера.** ✅ Аудит 05.07.2026 → `audit_report_section6_codebase.md` Где: сервер `~/TWC/TwoComms_Site` (в одной SSH-сессии: `git status`, `git stash list`, `git log -3`). Что: деплой = `git pull` — проверить, нет ли на сервере незакоммиченных правок (правки «на бою» будут конфликтовать с pull и молча теряться).
- [x] **CB-044. Crontab-инвентаризация.** ✅ Аудит 05.07.2026 → `audit_report_section6_codebase.md` Где: сервер `crontab -l` (одна SSH-се��сия). Что: полный список задач → таблица «задача → команда → скрипт существует в репо? → лог»; выявить cron-задачи, ссылающиеся на удалённые/переименованные скрипты (тихо падают).
- [x] **CB-045. Логи сервера и ротация.** ✅ Аудит 07.07.2026 (SSH, counts-only): `logs/` = 958 MB, `nova_poshta_cron.log` = 826,93 MB, `django.log.pre-rotation.bak` = 130 MB, user logrotate не найден; password hits 0, но PII-like hits есть в django/stderr/recover/nova_poshta, `rum.log` даёт 24 secret-like hits без раскрытия строк; нужен rotate+маскирование PII → `audit_report_section6_codebase.md`

---

## РАЗДЕЛ 7. МАТРИЦА РИСКОВ ПРИ ПРОВЕДЕНИИ РАБОТ

Каждый агент ОБЯЗАН свериться с этой таблицей пе��ед выполнением пункта. Правило: если действие попадает в риск-класс — сначала митигация, потом действие.

| ID | Риск | Триггер (какие пункты) | Вероятность/Ущерб | Митигация |
|---|---|---|---|---|
| RISK-01 | Удаление «мёртвого» файла, который дергается cron-ом на сервере | CB-003, CB-015, TD-001…007 | Средняя / Высокий (тихий отказ фидов, синков) | СНАЧАЛА CB-044 (crontab-инвентаризация), потом любые удаления |
| RISK-02 | SSH rate-limit: серия подключений блокирует доступ | все SSH-проверки | Подтверждено 05.07.2026 / Средний | Батчить ВСЕ команды в один вызов; пауза ≥ 60s между сессиями; не параллелить |
| RISK-03 | Правка checkout/monobank без тестов ломает приём денег | CRO-040…047, CB-020 | Средняя / Критический | Сначала CB-024 (смок-тесты), фиксы — мелкими PR, тест-заказ после каждого деплоя |
| RISK-04 | Массовая чистка except/print меняет поведение (код полагается на глотание ошибок) | CB-020, CB-021 | Высокая / Средний | Только добавлять логирование, НЕ менять control flow; никаких авто-замен по всему репо |
| RISK-05 | Чистка CSS/JS ломает страницы, стили которых видны только в проде | CB-030…034 | Высокая / Средний | PurgeCSS только с whitelist динамических классов (JS-генерируемые, admin); скриншот-сравнение до/после на 5 ключевых шаблонах |
| RISK-06 | git filter-repo/переписывание истории ломает клоны и сервер | CB-001 | Низкая / Критический | Только `git rm --cached` + .gitignore; filter-repo — отдельное согласованное окно |
| RISK-07 | Миграции на бою без бэкапа | TD-030, CRO-041 (новые поля Order) | Средняя / Критический | TD-020 (бэкап) обязателен перед КАЖДОЙ миграцией; миграции только аддитивные (add column NULL), без drop/rename в том же релизе |
| RISK-08 | Двойная точка правды settings.py vs production_settings.py: фикс не того файла | CB-012, любые правки настроек | Высокая / Высокий | Перед правкой настроек выяснить фактический DJANGO_SETTINGS_MODULE на сервере (одна SSH-сессия) |
| RISK-09 | Правка middleware-порядка ломает UTM/кэш/сессии | CB-042, AN-030…036 | Средняя / Высокий | Порядок менять только с интеграционным тестом «utm → login → заказ»; по одному изменению за раз |
| RISK-10 | Включённый runtime-оптимизатор изображений съедает память shared-хостинга при нагрузке | CB-041 | Средн��я / Высокий | Проверить флаг в проде до любых экспериментов с медиа; нагрузочный тест не на бою |
| RISK-11 | Удаление «неиспользуемого» пакета, который импортируется лениво/условно | CB-016 | Средняя / Средний | grep по всем веткам импортов (importlib, try/except ImportError — их 697!); удалять по одному пакету на релиз |
| RISK-12 | Тест-заказы и тест-события загрязняют боевую аналитику | CRO-050, AN-010…021 | Высокая / Средний | Все тест-прогоны с utm_source=audit + ��ометка заказа; test_event_code для пикселей (AN-015); после аудита — очистка тест-записей по согласованию |
| RISK-13 | Правки текстов/SEO стирают наработанные позиции | AEO-004, SEO-007 | Средняя / Высокий | Перед переписыванием title/description зафиксировать текущие позиции (GSC-экспорт); менять батчами по 10 страниц с контролем через 2 недели |
| RISK-14 | «Оптимизация» дублей img/ vs images/ бьёт по кэшированным URL в GMC-фиде/пикселях | CB-013 | Средняя / Средний | Сначала карта внешних потребителей URL (фиды, og:image), редиректы со старых путей |
| RISK-15 | Секреты утекают в отчёты/коммиты при аудите | все | Низкая / Критический | Перед каждым коммитом `git diff` на пароли/токены/IP; SSH-реквизиты нигде не фиксируются письменно |

---

## ПОРЯДОК ВЫПОЛНЕНИЯ (ПРИОРИТЕТЫ)

0. **P0 — предохранители (делать ПЕРВЫМИ, до любых фиксов):** CB-044 (crontab), CB-043 (git-состояние сервера), CB-012/RISK-08 (какой settings-модуль боевой), TD-020 (бэк��п БД), CB-024 (смок-тесты на деньги). Без них любой следующий шаг — игра в рулетку.
1. **P0 — данные (без этого всё остальное слепо):** CRO-041, CRO-042, AN-031, AN-013, AN-035, CRO-050 (acceptance), TD-033. Соот��етствуют TECH-060…063.
2. **P0 — верификация пикселей:** AN-010…012, AN-020, CRO-045 (TECH-064, TECH-066).
3. **P1 — статусная модель и точно��ть:** TD-030…032, AN-014 (TECH-070…074).
4. **P1 — SEO-критика:** SEO-020 (Product schema), SEO-007, SEO-008, SEO-003.
5. **P1 — быстрый тех. долг с высокой отдачей:** CB-004 (backup-файлы), CB-030/031 (CSS-диета: минификация), CB-032 (изоляция конфигуратора), CB-040 (пин версий), CB-021 (print→logger в orders/).
6. **P2 — CRO/UX:** разделы 1.1–1.5 по порядку; замер CRO-051 до/после.
7. **P2 — гигиена репозитория:** CB-001…003, CB-005…007, CB-013 (по RISK-14), TD-001…007.
8. **P2 — AEO и декомпозиция:** AEO-001…006, CB-022 (план, не исполнение).

## КАК ФИКСИРОВАТЬ БАГИ

- Найден баг → запись в журнал ниже: `дата | ID пункта | что найдено (факт, шаги воспроизведения) | severity P0–P2`.
- Фикс → отдельная ветка/коммит `fix(ID): описание`, ссылка на PR в журнале, пункт → `[x]`.
- Изменения в БД (миграции) — только после ревью владельцем; на бою сначала бэкап (TD-020).
- Данные для сверки брать ТОЛЬКО из живой MySQL через SSH (read-only), не из локальной SQLite.

## ЖУРНАЛ НАХОДОК И ВЫПОЛНЕНИЯ

| Дата | ID | Находка / что сделано | Severity | Коммит/PR |
|---|---|---|---|---|
| 16.07.2026 | F-036 / TECH-069 | `48d1c9fe`: bounded retry for transient Telegram text failures and truthful failure state deployed; local/server 36/36, check/compile/getMe/storefront passed; `[o]` until natural notification evidence, duplicate-after-lost-response and daemon-exit residuals documented | P2 partial | [AUDIT_FINDINGS §F-036](../../docs/qa/AUDIT_FINDINGS_2026-07-09.md#f-036--telegram-admin-notify-intermittent-disconnect) |
| 16.07.2026 | F-035 / TECH-082 | `341d42a9`: CSP telemetry receiver/log isolation deployed and verified (local/server 16/16; live 204/204, private parseable JSON log); header/allowlist unchanged, 0 real reports, observation >=24h from 16:07:50 UTC remains | P2 partial | audit_report_section4_seo.md |
| 06.07.2026 | CRO-045 | ��атрица purchase-моментов (4 слоя × 3 потока): **P0 — COD-покупку видит только Meta CAPI через NP-крон; GA4/TikTok/UserAction — никогда**; живой писатель UserAction purchase — `record_order_action` (monobank.py:1241, paid+prepaid), `record_purchase` мёртв; внутренний lead = создание ин��ойса до оплаты; prepaid = полная сумма во всех слоях, refund-событий нет; TikTok paid без pre-check дедупа; целевое определение для TECH-066 зафиксировано | P0 done | audit_report_checkout_critical.md |
| 06.07.2026 | CRO-046 | **P0: COD-чекаут — промокод не применяется никогда** (мёртвый ключ сессии + несуществующие поле/метод в checkout.py:224-235; клиент видит скидку, платит полную сумму); **P0: per-user/групповые лимиты промо не работают** (`record_usage` — 0 call-sites, PromoCodeUsage пуст); P1: завышенный остаток при prepay_200+промо, лимит сжигается при создании инвойса; P2: `coupon_apply` события нет (TECH-023); ошибки apply-endpoint — понятные (требование пункта ок) | P0 done | audit_report_checkout_critical.md |
| 06.07.2026 | CRO-047 | Базовые требования ОК (пустая корзина, atomic, нет 500); **P1: исчезнувший товар молча выбрасывается из заказа (оба пути); COD без guard total_sum<=0 → заказ на 0 грн возможен; складского контроля нет как класса**; P2: внешний Monobank API + clear_cart внутри atomic (потеря кор��ины без заказа при сбое, cached_db сессии не откатываются); P3: стаб monobank_webhook | P1 done | audit_report_checkout_critical.md |
| 05.07.2026 | CRO-041 | Аудит выполнен: COD не пишет UTM/session_key, `link_order_to_utm` не имеет достаточного fallback даже для monobank; TECH-060 детализирован | P0 done | audit_report_section1_cro.md |
| 05.07.2026 | CRO-042 | Аудит выполнен: 0/1015 converted; причина — COD без трекинга + lookup UTMSession по одному session_key; TECH-061 детализирован | P0 done | audit_report_section1_cro.md |
| 05.07.2026 | TD-033 | Аудит выполнен: 28/28 CustomPrintLead `new`, смена статуса не встроена в процесс, связки с заказами нет; TECH-062 детализирован | P0 done | audit_report_section3_techdebt.md |
| 05.07.2026 | AN-013 | Аудит выполнен: click-ID не доходят до CAPI для COD, fbclid без UTM может теряться; расширение TECH-060 детализировано | P0 done | audit_report_section2_analytics.md |
| 05.07.2026 | AN-031 | Аудит выполнен: `cycle_key()` при login рвёт `session_key`→UTMSession; fallback на visitor/session описан | P0 done | audit_report_section2_analytics.md |
| 05.07.2026 | AN-032 | БД: utm_source дубли ig/Instagram/IGShopping/Inst_Vid; сырой ad id как source | P1 | — |
| 05.07.2026 | AN-035 | Аудит выполнен: `is_bot` мёртв по дизайну, `record_user_action` пишет ботские product_view без SiteSession; TECH-063 детализирован | P1 done | audit_report_section2_analytics.md |
| 05.07.2026 | AEO-001 | БД: 109 UTM-сессий с utm_source=chatgpt.com — AI-канал уже 3-й по объёму | P1 | — |
| 05.07.2026 | TD-030 | БД: статусы заказов только done/cancelled — нет shipped/delivered/RTS | P1 | — |
| 05.07.2026 | SEO-020 | В product_detail.html не найден JSON-LD Product (grep по репо) — требуется подтверждение рендером | P1 | — |
| 05.07.2026 | TD-011 | Redis live-check: cache roundtrip OK на бою | info | — |
| 05.07.2026 | CB-001 | 132 файла артефактов (PNG/JSON) в git; репо 328MB без .git; личное фото BrandDNA/me.JPG | P2 | — |
| 05.07.2026 | CB-002 | 202 md-отчёта в корне репозитория | P2 | — |
| 05.07.2026 | CB-003 | 30 py + 19 sh loose-скриптов в корне, из них 6 конкурирующих deploy-скриптов | P1 | — |
| 05.07.2026 | CB-004 | 4 backup-файла под git: views.py.backup (7790 строк), styles.css.bak2 (445KB), order_success_old.html, tmp_old_index.html | P1 | — |
| 05.07.2026 | CB-005 | xlsx с оптовыми/закупочными ценами лежат в репозитории | P1 | — |
| 05.07.2026 | CB-020 | 697 широких except в Python-коде — ошибки глотаются | P1 | — |
| 05.07.2026 | CB-021 | 120 print() в боевом коде вместо logging | P2 | — |
| 05.07.2026 | CB-022 | management/views.py 8188 строк; storefront/views/admin.py 3270; models.py 2935 — god-files | P2 | — |
| 05.07.2026 | CB-023 | 56 console.log в боевом JS | P2 | — |
| 05.07.2026 | CB-024 | 177 тест-файлов, но чекаут/корзина/UTM почти без тестов — деньги не покрыты | P0 | — |
| 05.07.2026 | CB-030 | styles.css 565KB + management.css 234KB + configurator 160KB; CSS суммарно 3.5MB | P1 | — |
| 05.07.2026 | CB-040 | openai/google-auth/google-analytics-data без пина версий в requirements.txt | P1 | — |
| 05.07.2026 | CB-042 | 26 middleware в цепочке с хрупкими зависимостями порядка | P2 | — |
| 05.07.2026 | TD-023 | django-ratelimit установлен, @ratelimit в коде не найден (0 использований) — но есть свой SimpleRateLimitMiddleware | P2 | — |
| 05.07.2026 | RISK-02 | Сервер сбрасывает частые SSH-подключения (kex reset) — rate-limit подтверждён; батчить команды в одну сессию | info | — |
| 05.07.2026 | CB-013 | Дубли static/img (12MB) и static/images (6.3MB) | P2 | — |
| 05.07.2026 | CB-014 | dtf-app глобально переопределяет команду collectstatic | P2 | — |
| 05.07.2026 | CB-044 | Аудит crontab: 7 задач, все скрипты в репо; НЕТ бэкап-cron, НЕТ feed-cron; логи в logs/ и tmp/ | P0 done | audit_report_section6_codebase.md |
| 05.07.2026 | CB-043 | Git сервера: tracked чисто, 10 stash (возможна потерянная работа), untracked диаг-скрипты на бою | P0 done | audit_report_section6_codebase.md |
| 05.07.2026 | CB-012 | Боевой settings = twocomms.production_settings (passenger_wsgi); на сервере .env И .env.production; env-флаг DISABLE_ANALYTICS может отключать UTM-мидлвари — проверить значение | P0 done | audit_report_section6_codebase.md |
| 05.07.2026 | CB-040 | Боевые версии: openai==2.30.0, google-auth==2.52.0, google-analytics-data==0.22.0 — пин исполнителю | P1 done | audit_report_section6_codebase.md |
| 05.07.2026 | TD-020 | **P0: регулярных бэкапов MySQL НЕТ; последний ручной дамп от 24.10 (>8 мес). Блокирует все миграции (RISK-07)** | P0 | audit_report_section3_techdebt.md |
| 05.07.2026 | TD-016 | Ротация django/stderr есть (5 покол.); 8 мёртвых логов; ai_generation.log мёртв с 09.2025, image_optimization.log с 10.2025 | P2 partial | audit_report_section3_techdebt.md |
| 05.07.2026 | CB-024 | Уточнение баз. линии: 183 тест-файла, storefront/tests 68 файлов/18.5k строк (checkout/cart/utm покрыты юнитами); ДЫРЫ: orders/ 0 тестов (CAPI 850 строк!), accounts/ 0, вебхук-подпись monobank без тестов, идемпотентность 1 тест, CI отсутствует, тесты на SQLite vs боевой MySQL | P0 done | audit_report_section6_codebase.md |
| 05.07.2026 | CRO-001 | Живой рендер + шаблон: спорт/бейсбол-лексики нет, «я»-лексики нет; находки: футболки первыми в title/hero-subtitle/заголовке витрины (3 строки копирайта), первые 5 карточек витрины — футболки (admin priority), концепция «труднощі» текстом не выражена | P1 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-002 | **P0-БАГ найден:** mobile 360px — hero-CTA («Дивитись каталог», «Створити свій принт») обрезаны overflow:hidden (hero height:60vh при контенте 727px, base.html:517), клики перекрыты survey-секцией; PWA-prompt закрывает 45% первого экрана; desktop и все целевые URL — OK (200, 0 редиректов); кандидат №1 в причины ATC=0,12% | P0 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-003 | Lab-базовая линия: LCP 1.78s (текстовый span hero), CLS 0.00, FCP 692ms, TTFB mobile 518ms / desktop 290ms — зелёная зона (без троттлинга); CrUX API 403 → PSI field-замер вручную владельцу; после фикса CRO-002 CLS перепроверить (держать min-height) | P1 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-004 | Подтверждено: атрибуция ЛОМАЕТСЯ — utm/fbclid fast-path нет (grep=0), GTM home грузится по click/keydown (scroll не считается) или 25–35s, analytics-loader 25s; paid-bounce невидим для Meta/TikTok/GA4, _fbc не создаётся; фикс P1 ~10 строк в base.html | P1 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-005 | Блока отзывов на главной нет; app reviews/ готов и работает на PDP (approved-список, aggregate rating) — TECH-013 = переиспользование готового, P2 | P2 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-006 | Мёртвого DOM на главной нет (един. display:none — легитимный load-more); survey-блок под флагом; рекомендация P2 — survey-секцию ниже товаров | P2 done | audit_report_section1_homepage.md |
| 05.07.2026 | CRO-010 | Фильтры категория/цвет работают, URL-шаримы; фильтра по РАЗМЕРУ не существует; всё full-page reload (не AJAX); slug/id кат��горий сверены с БД | P2 done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-011 | **P1-БАГ×2:** (1) пагинация `?page=N` затирает `?color=` — live-подтверждено; (2) `/catalog/?page=2..N` индексируемые (index,follow + self-canonical) дубли БЕЗ товаров — корень не рендерит грид, paginator строится зря; категорийная пагинация чиста (0 дублей между стр.) | P1 done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-012 | lazy/width/height OK на 100% карточек; eager лишь 2 из первого ряда (реком. 4); у 9/16 карточек нет srcset/AVIF — media/product_colors/ без optimized-вариантов | P2 done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-013 | Подтверждено БД+рендер: top-5 priority — все футболки; max priority tshirts 68 > hoodie 63 > long-sleeve 51; showcase-хардкод ставит футболки выше худи; Category.order все 0 | P1 done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-014 | Механики налич��я нет: 75/75 ProductColorVariant stock=0 (поле мертво, нигде не читается), размерного склада нет; «наличие»=ручной status=published; бейджи наличия внедрять нельзя до решения по инвентаризации | P1 done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-015 | N+1 не�� (боевой замер): каталог 14–19 SQL, цвет-фильтр 12; повторный хит из page-cache = 0 SQL; бонусы: ?sort= мёртвая ветка (view не читает), DEBUG=True роняет прод (compressor OfflineGenerationError), /search/ без пагинации | info done | audit_report_section1_catalog.md |
| 05.07.2026 | CRO-020 | 10/10 случайных товаров: БД↔сайт полное совпадение (title/final_price/og:price/категория); published=65 (не 68); RU/EN description — заглушки ~120 симв. (P2); двойной product_view на legacy `?size=` URL — record_product_view до 301 (P1, → CRO-024); на живой PDP 5 JSON-LD блоков — гипотеза SEO-020 «нет schema» опровергнута | P1 done | audit_report_section1_product.md |
| 05.07.2026 | CRO-021 | Первого лица владельца в опи��аниях НЕТ: 65/65 PDP просканированы, все 27 совпадений — названия принтов в кавычках + regex-артефакт «ім'я»; спорт/бейсбол-лексики 0; tone of voice соответствует концепции «difficulties»; побочно P2: 65/65 описаний содержат идентичный boilerplate («це не просто одяг. Це стан…») — near-duplicate content риск; SSH временно блокируется rate-limit'ом после серии сессий — метод: скан живых страниц из sitemap | info done | audit_report_section1_texts.md |
| 05.07.2026 | CRO-022 | Alt-тексты: 65/65 уникальны, непусты (шаблон «título — принт, тип TwoComms»); фото 65/65; механизм webp/avif+srcset спроектирован правильно; НО P1: 20/65 PDP — LCP-`<img fetchpriority=high>` грузит оригинал 100–276 KB (худший: PNG 276 KB), optimized/*-файлы 404 — не сгенерированы из-за старого bind-бага inline-fallback (признан в docstring tasks.py); фикс кода уже есть, backfill НЕ выполнен → задача: `manage.py optimize_images` на сервере + алерт в audit_product_images | P1 done | audit_report_section1_images.md |
| 05.07.2026 | CRO-023 | Product report дополнен: лонгсливы без size-grid на PDP и `/rozmirna-sitka/`, `view_size_guide` отсутствует, seo-pricing ссылки `/product/{id}/` дают 404; задачи SizeGrid/catalog/get_absolute_url/TECH-005 | P1 done | audit_report_section1_product.md |
| 05.07.2026 | CRO-024 | Product report дополнен: завышение product_view объясняется bot-hit UserAction без bot-filter, записью до 301-редиректа и отсутствием дедупа; сервер+JS дубля нет | P1 done | audit_report_section1_product.md |
| 05.07.2026 | CRO-025 | Product report дополнен: UX выбора цвета/размера работает частично; цена по `price_override` не обновляется, stock не блокируется, select_size/select_color не трекаются | P1 done | audit_report_section1_product.md |
| 06.07.2026 | CRO-030 | Архитектура корзины задокументирована: session-first (`session['cart']` + `custom_print_cart`) + UserCart JSON-снапшот с ревизией sha256; merge на login без дублей (qty складываются / lead_id уникален). Находки: **P2-БАГ `context_processors.py:57` — ключ `custom_cart` вместо `custom_print_cart` (бейдж 0 при кастом-only)**; P2 `/cart/count/` не считает кастом (расходится с `cart_summary`); P2 GET `cart_summary` мутирует сессию и сбрасывает monobank-инвойс через `_reset_monobank_session(drop_pending=True)`; P2 multi-device lost-update (whole-snapshot LWW, merge только при логине) | P2 done | audit_report_section1_cart.md |
| 06.07.2026 | CRO-031 | Сценарии проверены: двойной клик не создаёт 2 позиции (мерж по ключу), удаление последней позиции обнуляет бейдж, смена qty пересчитывает сумму+промокод. Находки: **P1 — `update_cart`/`remove_from_cart` не вызывают `_reset_monobank_session` → pending-инвойс со старой суммой остаётся оплачиваемым при сбое JS-refreshCartSummary**; P2 — нет in-flight guard/disable у `[data-add-to-cart]` в main.js (qty удваивается, пиксели дублируются); P2 — fallback `remove_from_cart` (cart.py:981) удаляет ВСЕ варианты товара при рассинхроне ключа; P3 — qty без стока, мёртвый код (813, 1067), смешанный контракт ok/success | P1 done | audit_report_section1_cart.md |
| 06.07.2026 | CRO-032 | Page-cache = самописный `cache_page_for_anon` (только анонимы, home/каталог 600s, Redis, PDP выключен); бейдж cache-safe (SSR=0 + AJAX no-store + localStorage-хинт); `/cart/` never_cache — live OK. Находки: **P1 (подтверждён live: 2 curl-сессии получили ОДИН csrfmiddlewaretoken при разных csrftoken-cookie, POST /i18n/setlang → 403) — кэшированный HTML отдаёт чужой CSRF-токен → language switcher в футере ломается для всех анонов на cache-hit**; P2 — `cart_summary`/`get_cart_count` без no-store (LiteSpeed впереди); P3 — `user_state_hint` мёртв (не в context_processors → ко��рекция CRO-030 №1: прод-эффекта нет); P3 — главная/каталог без Cache-Control вообще; P3 — неиспользуемые опасные декораторы `cache_views.py` (public + нестабильный hash-ETag). Смягчения, которые нельзя ломать: пустой meta csrf-token + заполнение из cookie, get_token() на cache-hit | P1 done | audit_report_section1_cart.md |
| 06.07.2026 | CRO-033 | Цепочка 1 клика задокументирована: 1 POST /cart/add/ → 1 UserAction (record_add_to_cart) + 1 fbq AddToCart (eventID) + 1 ttq (event_id) — общий event_id, дедуп-задел есть; inline onclick="AddToCart" в шаблонах нет → ui-fallback мёртв, дублей fetch нет. Находки: **P2 — dataLayer получает 2 события на клик (`AddToCart` из trackEvent-ветки gtag/GTM + канонический `add_to_cart` из pushAddToCartEvent) — риск двойного GA4-счёта, проверить триггеры GTM-контейнера**; **P2 — server-side CAPI нет вообще (0 вызовов graph.facebook в *.py): при блокировщиках ATC теряется полностью — кандидат в объяснение 36009 views → 44 ATC (CRO-051)**; P2 — двойной клик = 2 UserAction + 2 пикс. события с разными event_id (закрывает guard из CRO-031); P3 — кастом-принт: пиксельный AddToCart есть, серверного record_add_to_cart нет (рассинхрон воронки); P3 — ранние TikTok-события до _ttqLoaded могут теряться. Live Test Events (Meta/TikTok) — ручной шаг, нужен доступ к кабинетам | P2 open | audit_report_section1_cart.md |
| 06.07.2026 | CRO-050 | Живой прогон воронки (curl, UA Chrome, utm_source=audit): home→каталог→PDP→POST /cart/add/ (200 ok, cart_count=1)→корзина с товаром; CSRF/сессии/AJAX работают; сабмит заказа не делался. Находки: offer_id с рус. цветом «ЧЕРНЫЙ» (NEW-406); pay_type в UI только online_full/prepay_200, legacy-дефолт 'cod' в checkout.py:119 (NEW-407). Остаток: SSH-сверка UTMSession/UserAction по sessionid=jrg8b3… (батч в AGENT_WORK_LOG_iter4.md §6) | P2 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AN-002 | Код-подтверждение: fast-path utm/fbclid/gclid/ttclid в base.html/analytics-loader.js отсутствует (grep=0); фикс = CRO-004 | P1 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AN-032 | Код-подтверждение: словаря нормализации utm_source нет ни в одном utm_*.py; фрагментация ig/Instagram/… пишется в БД как есть | P1 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AN-033 | Код-подтверждение: детекции AI-рефереров в трекинге нет (Perplexity/ChatGPT только в seo_utils.py-комментариях); нужен канал «AI» (TECH-065) | P2 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AN-034 | Полного page-cache middleware нет; UTMTrackingMiddleware в цепочке после Session (settings.py:223); live-прогон с ?utm_ прошёл через мидлварь (выданы sessionid/twc_vid); kill-switch DISABLE_ANALYTICS в production_settings.py | info done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AN-040 | Используется admin-дашбордом (admin_analytics.py); обвязка GA4/Clarity защитная (TTL/бюджеты/SWR 30д); ключа в git нет; **NEW-405: авто-дискавери *service*account*.json в каталоге проекта — проверить webroot на сервере** | P2 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | NEW-403 | utm_middleware.py:153-192 пишет ip_address+гео (country/city/region/tz) в UTMSession; шаблона политики приватности с описанием этого не найдено в templates/pages/ | P2 open | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | NEW-404 | trim_analytics.py чистит только PageView/SiteSession (90д); UTMSession и UserAction НЕ чистятся → неограниченный рост (та же причина отключения аналитики в мае 2026 по докстрингу) | P1 open | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | SSH | Сервер сбрасывает SSH (kex reset) всю сессию — серверный батч (CRO-051 baseline, AN-004/039 остатки, TD-011/013/014) НЕ выполнен; готовый батч-скрипт: AGENT_WORK_LOG_iter4.md §6 | blocker | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | SEO-001..005 | Live-аудит: robots.txt корректный (+AdsBot-блоки, трекинг-параметры закрыты); все 8 sitemap 200 (562 URL суммарно), выборка URL живая; canonical на месте (многострочный тег!), ?utm→чистый canonical; hreflang uk/ru/en+x-default взаимный на 4 шаблонах; H1=1 везде. NEW-410: lastmod отсутствует в sitemap-products (1 на 195 URL) | P2 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | SEO-020 | ОПРОВЕРГНУТО: Product+Offer+Brand+Shipping+ReturnPolicy JSON-LD на живой PDP есть (генерит seo_utils.py, не шаблон) — TECH-030 не нужен. NEW-411: site-wide NewsArticle/GovernmentOrganization-граф в Organization schema (seo_utils.py:1518) — bloat, P3 | P1 done | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | SEO-006 | Несуществующий /product/…/ отдаёт 404 (не 410); краулинг внутренних ссылок и проверка реально удалённых товаров ��� остаток | P3 partial | AGENT_WORK_LOG_iter4.md |
| 06.07.2026 | AEO-002 | Live-проверка: /llms.txt и /llms-full.txt уже существуют (оба 200), контент качественный — Brand facts (Founded: 2022, Kharkiv), Commerce facts, канонические маршруты, правила цитирования для AI; robots.txt не противоречит; отдаются кодом (urls.py:62-64 + static_pages.py) с регресс-тестами. Создавать нечего — TECH-035 закрыт ранее | P2 done | iter5 |
| 06.07.2026 | AEO-005 | Ложного «с 2014» нет: repo-grep (html/py/js/txt) + live-скан 9 ключевых страниц чистые; llms.txt даёт Founded: 2022. Единственное вхождение — placeholder `EST. 2014` (пример текста рукава) в custom_print.html:648 → заменён на `EST. 2022` + locale en/ru django.po (после деплоя нужен compilemessages). Остаток: grep «2014» по БД-оп��саниям — в SSH-батче | P2 done | iter5 |
| 06.07.2026 | AEO-003 | Q&A-структура подтверждена (код+live): per-product FAQPage JSON-LD с дедупом против глобального /faq/; live /faq/ 31 хит FAQPage/acceptedAnswer; HTML-таблицы замеров на PDP (tc-size-table) и /rozmirna-sitka/ (2 шт); сроки «1–3 робочі дні» прямо на PDP. Остатки: лонгс��ивы без таблицы (=CRO-023), прямой ответ «Из чего?» в 1-м абзаце не системный (P3, → переписывание boilerplate CRO-021) | P2 done | iter5 |
| 06.07.2026 | AEO-004 | Grep-аудит тона: «бейсбол» 0; первое лицо владельца 0 (все «мені» = принт «мені поЖуй»); БД-описания чисты (CRO-021). Список на переписывание (P3, 2 строки): _product_themes.py:116,120 — слово «спорт» в перечислении аудитории «last breath»; custom_print.html:978 «спортивна команда» — легитимный B2B-кейс, не трогать | P2 done | iter5 |
| 06.07.2026 | AEO-006 | AI-автогенерация: `product_seo_autofill.py` БЕЗ AI (шаблоны, идемпотентно, факты состава выверены по категориям — hoodie 320 г/м²); OpenAI только в translate_products.py (переводы, mirror-fallback) и seo_utils.py (категорийные keywords, graceful без ключа). P3: «мілітарний» в autofill-шаблонах для всех категорий — выборочная вычитка. Остаток: ai_generation.log на сервере — в SSH-батче | P2 done | iter5 |
| 06.07.2026 | CRO-051 | БЛОКЕР: SSH-батч (baseline воронки из UserAction + grep «2014» по БД + ai_generation.log) не выполнен — сервер весь день сбрасывает соединения (kex reset) на серии попыток с бэкоффом до 5 мин; готовый read-only скрипт: `.v0/audit/cro051_baseline.py` (запуск: `python manage.py shell < cro051_baseline.py` в 1 сессию) | blocker | iter5 |
| 07.07.2026 | SEO-008 | Фид динамический и живой (384 оффера, 65 групп, поля полные, картинки 200); P1 — все g:link 301 с потерей color; P2 — availability=in_stock захардкожен; P3 — gtin/identifier_exists/shipping/дубль-URL/нет кэша; пустой статический xml в корне — артефакт | P1 found | audit_report_section4_seo.md |
| 07.07.2026 | SEO-009 | Интеграции добротные (on_commit/retry/host-фильтр/audit-log, креды не в git); P2 — сигнальный путь Google без дедупа/quota_limit (масс-пересохранение сжигает квоту 200/день); P3 — нет changed-fields, лишний SELECT, retention audit-таблицы | P2 found | audit_report_section4_seo.md |
| 07.07.2026 | SEO-021 | Разметка без фейков: aggregateRating только при ≥1 APPROVED из единого источника, хардкодов нет, ProductGroup чист, тесты есть; live 0 отзывов → схема молчит корректно; P3 — мёртвый product_detail_new.html | clean | audit_report_section4_seo.md |
| 07.07.2026 | SEO-006 | Полный краул 489/489: все sitemap-URL 200 без редиректов, 0 битых внутренних ссылок (39 вне-sitemap все 200), несуществующие URL всех типов → чистый 404 (не 410, P3) | clean | data/seo_crawl_analysis_report.md |
| 07.07.2026 | SEO-007 | Полный краул 489/489: 0 missing title/desc/canonical/OG/H1, canonical=final везде; P3-качество: 41 title >65, 121 desc >165, 13 групп дублей (3 товара Reality Bends шарят title в ru/en; 11 en-блогов с непереведёнными title) | P3 found | data/seo_crawl_analysis_report.md |
| 16.07.2026 | F-028 / SEO-007 | `da910c46`: locale runtime fixed; local/server 16/16 + API routes 7/7 + check clean, live 39/39 aligned, health 200; no migration/data mutation; owner naming map remains | partial | audit_report_section4_seo.md |
